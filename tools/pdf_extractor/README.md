# PDF Metrics & Resources Extraction

Скрипт для автоматического извлечения метрик и ресурсов из официальной PDF документации Huawei OceanStor.

## 📁 Файлы

- `extract_metrics_resources_from_pdf.py` - основной скрипт парсинга
- `OceanStor Dorado V700R001C10 REST Interface Reference.pdf` - последняя версия документации
- `OceanStor Dorado V700R001C00 REST Interface Reference.pdf` - предыдущая версия
- `OceanStor Dorado 6.1.8 REST Interface Reference.pdf` - старая версия

## 🎯 Назначение

Скрипт извлекает из раздела **Appendix: Performance Indicators** PDF документации:

1. **Метрики (Metrics)**: ID + название из строк таблиц
2. **Ресурсы (Resources)**: ID из строк "Type" в заголовках таблиц
3. **Сравнение**: автоматическое сравнение с существующими словарями `METRIC_DICT.py` и `RESOURCE_DICT.py`

## 📊 Результаты последнего запуска (2025-12-02)

```
📊 МЕТРИКИ (из 3 PDF файлов):
   Всего уникальных в PDF: 427 метрик
   METRIC_DICT.py: 922 метрик (из реальных .dat файлов + PDF)
   ✅ Общих: 427 (100% coverage PDF → DICT)
   ✅ Только в DICT: 495 метрик (из реальных .dat файлов)
   
📦 РЕСУРСЫ:
   PDF: 34 ресурса
   RESOURCE_DICT.py: 70 ресурсов (из реальных .dat файлов + PDF)
   ✅ Общих: 34 (100% coverage PDF → DICT)
```

**Вывод**: Все метрики из PDF документации (427) теперь присутствуют в словаре. Дополнительно словарь содержит 495 метрик из реальных `.dat` файлов, которые отсутствуют в PDF!

## 🚀 Использование

### Базовый запуск

```bash
cd /data/projects/monitoring_VM_Grafana
python3 tools/pdf_extractor/extract_metrics_resources_from_pdf.py
```

### Параметры в скрипте

Скрипт настраивается через константы:

```python
# Путь к PDF файлу
PDF_PATH = Path("tools/pdf_extractor/OceanStor Dorado V700R001C10 REST Interface Reference.pdf")

# Выходной JSON
OUTPUT_JSON = Path("temp/simple_metrics_resources.json")

# Диапазон страниц Appendix
APPENDIX_START = 4500
APPENDIX_END = 4712
```

### Вывод

Скрипт создает JSON файл с результатами:

```json
{
  "metrics": {
    "22": {
      "id": "22",
      "name": "IOPS(IO/s)",
      "pages": [4537, 4591, 4624, 4678]
    },
    ...
  },
  "resources": {
    "10": {
      "id": "10",
      "pages": [4536, 4623]
    },
    ...
  },
  "comparison": {
    "metrics": {
      "pdf": 291,
      "dict": 818,
      "common": 219,
      "new": [382, 383, ...],
      "missing": [...]
    },
    "resources": {
      "pdf": 34,
      "dict": 54,
      "common": 29,
      "new": [824, 1006, ...],
      "missing": [...]
    }
  }
}
```

## 📝 Логика работы

### 1. Извлечение метрик

Для каждой таблицы в Appendix:
- Ищет строки с паттерном: `[Название метрики, ID, ...]`
- ID должен быть числом от 2 до 100000
- Название должно быть длиннее 3 символов
- Фильтрует служебные строки (Indicator, Type, Statistics Type)

**Пример строки метрики:**
```
| Read Bandwidth (MB/s) | 23 | √ | √ | √ | ... |
```
→ Извлекается: ID=23, Name="Read Bandwidth (MB/s)"

### 2. Извлечение ресурсов

Ищет строки "Type" в таблицах:
```
| Type |   | 207 | 11 | 216 | 279 | ... |
```
→ Извлекаются ID ресурсов: 207, 11, 216, 279, ...

### 3. Сравнение со словарями

Автоматически сравнивает результаты с:
- `parsers/dictionaries/METRIC_DICT.py` - 818+ метрик из реальных данных
- `parsers/dictionaries/RESOURCE_DICT.py` - 54+ ресурсов из реальных данных

Показывает:
- ✅ Общие (уже есть в словарях)
- 🆕 Новые (есть в PDF, нет в словарях)
- ⚠️ Пропущенные (есть в словарях, нет в PDF)

## ⚠️ Ограничения

1. **PDF документация неполная** - содержит только ~46% метрик (427 из 922)
2. **Названия метрик могут иметь артефакты переносов** - парсер корректно обрабатывает переносы строк
3. **Многостраничные таблицы** - парсер обрабатывает каждую страницу отдельно
4. **Нет связи метрики→ресурс** - скрипт собирает только списки, без привязки

## 🔧 Требования

```bash
pip install pdfplumber
```

**Версия Python**: 3.11+

## 📚 См. также

- `../../parsers/dictionaries/METRIC_DICT.py` - основной словарь метрик (922 записей)
- `../../parsers/dictionaries/RESOURCE_DICT.py` - основной словарь ресурсов (70 записей)
- `../../parsers/streaming_pipeline.py` - streaming парсер `.dat` файлов

## 🎯 Когда использовать

**✅ Используйте этот скрипт когда:**
- Вышла новая версия PDF документации Huawei
- Нужно проверить какие метрики добавились в новой прошивке
- Требуется обновить словари новыми ID из документации

**❌ НЕ используйте для:**
- Извлечения метрик из реальных данных (используйте streaming parser)
- Получения полного списка всех метрик (используйте существующие словари)

## 📊 История обновлений

### 2025-12-02 (Полная верификация)
- **Исправлено 48 ошибочных метрик** в диапазоне 90086-90142 (S3 Object Storage)
  - Пример: 90099 было "Initiate Multipart Upload OPS", стало "Avg. HeadObject Response Time"
- **Добавлено 20 новых метрик** из PDF документации:
  - 1225: AI Cache Hit Ratio (%)
  - 1700-1717: Deduplication/Compression метрики
  - 90058: Data Protection Capacity (MB)
- **Исправлено 5 ресурсов** в RESOURCE_DICT.py:
  - 1053: было "Controller SMB3", стало "Controller S3" (Amazon S3 протокол)
  - 824: SmartMove FsPair
  - 1006: SmartMove
  - 11110: LUN Clone
  - 11111: LUN Clone Group
- **Улучшен парсер**: корректная обработка ID с переносами строк (`129\n9` → `1299`)
- **Проверены все 3 PDF файла**: V700R001C10, V700R001C00, 6.1.8
- **100% покрытие**: все 427 метрик из PDF теперь в словаре

### 2025-10-30
- Добавлено 72 новые метрики из PDF в METRIC_DICT.py
- Добавлено 5 новых ресурсов в RESOURCE_DICT.py
- Создан упрощенный парсер без связи метрика→ресурс

---

**Автор**: AI Assistant  
**Дата создания**: 2025-10-30  
**Последнее обновление**: 2025-12-02

