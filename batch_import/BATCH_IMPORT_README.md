# Batch Import Script - Массовый импорт Performance логов

## 📋 Описание

Python скрипт для **последовательного массового импорта** архивов с Huawei performance данными в VictoriaMetrics через `huawei_streaming_pipeline.py`.

### Ключевые возможности

- ✅ Автоматическое обнаружение архивов `.zip` и `.7z` (рекурсивно в поддиректориях)
- ✅ **Поддержка вложенной структуры .7z** (автоматическое извлечение Perf ZIP)
- ✅ Извлечение серийных номеров массивов без полной распаковки
- ✅ Проверка наличия данных в VictoriaMetrics (пропуск уже импортированных)
- ✅ Real-time логирование процесса импорта
- ✅ Проверка успешности импорта (наличие данных в VM после обработки)
- ✅ Детальная статистика и финальный отчет
- ✅ Graceful shutdown (Ctrl+C) с сохранением прогресса
- ✅ Продолжение работы при ошибках в отдельных архивах
- ✅ Dry-run режим для предварительного анализа

### Поддерживаемые форматы архивов

| Формат | Описание |
|--------|----------|
| `.zip` | Прямые Perf архивы (содержат .tgz файлы с метриками) |
| `.7z`  | Data Collection архивы с вложенной структурой |

**Структура .7z архивов (Data Collection):**
```
Data_OceanStorDorado5000V6_20251023_<SN>.7z
  └── DataCollect/
      └── History_Performance_Data/
          └── <IP>/
              └── (<IP>)..._Perf_*.zip  ← извлекается автоматически
                  └── .tgz файлы с метриками
```

---

## 🚀 Быстрый старт

### Базовое использование

```bash
# Импорт всех архивов из директории (рекурсивно)
python3 batch_import.py /data/logs/perf/

# С использованием директории по умолчанию
python3 batch_import.py
```

### С пропуском уже импортированных

```bash
# Пропускает массивы, данные которых уже есть в VictoriaMetrics
python3 batch_import.py /data/logs/perf/ --skip-existing
```

### Dry-run режим (тестовый прогон)

```bash
# Анализирует архивы без реального импорта
python3 batch_import.py /data/logs/perf/ --dry-run
```

### С кастомным VictoriaMetrics URL

```bash
# Если VM на другом сервере
python3 batch_import.py /data/logs/perf/ --vm-url http://10.5.10.163:8428
```

### Комбинация опций

```bash
# Dry-run с проверкой существующих данных
python3 batch_import.py /data/logs/perf/ --skip-existing --dry-run
```

---

## 📊 Пример вывода

### Во время выполнения (.7z архивы)

```
================================================================================
🚀 BATCH IMPORT STARTED
================================================================================
Log directory: /data/sber/Logs for month reports and performance/perf/
VM URL: http://localhost:8428
Skip existing: True
Dry-run: False
================================================================================

✅ VictoriaMetrics доступна
🔍 Поиск архивов (.zip, .7z)...
✅ Найдено 14 архивов:
   - 7z:  14

[1/14] Обработка Data_Dorado6000V3_20251023163227_2102352KRR10KC000013.7z...
================================================================================
📦 Обработка: Data_Dorado6000V3_20251023163227_2102352KRR10KC000013.7z
================================================================================
🔍 Извлечение серийного номера...
✅ Серийный номер: 2102352KRR10KC000013
🔍 Проверка наличия данных в VictoriaMetrics...
⚠️  Данные для SN 2102352KRR10KC000013 не найдены в VictoriaMetrics
📦 Тип: 7z (вложенная структура)
📦 Извлекаю: DataCollect/History_Performance_Data/10.105.152.20/(10.105.152.20)D6000-TEST132SK_Perf_1761227876863.zip
🚀 Запуск streaming pipeline...
✅ Pipeline завершился успешно. Метрик отправлено: 37,751,040
🔍 Проверка импортированных данных в VictoriaMetrics...
✅ Данные в VM найдены. Последняя точка: 2025-10-24 03:00:00
⏱️  Время обработки: 38.8s

[2/14] Обработка следующего архива...
```

### Финальный отчет

```
================================================================================
📊 BATCH IMPORT SUMMARY
================================================================================
Total archives found:    14
Total archives processed: 14/14
  - Successful imports:   14
  - Failed imports:       0
  - Skipped:              0

Total time: 0h 8m 13s
Average time per archive: 35.2s

Arrays with data in VictoriaMetrics: 14
  - 2102352KRR10KC000013 (last data: 2025-10-24 03:00:00)
  - 2102352VUV10M3000129 (last data: 2025-10-24 03:00:00)
  - 2102352VUVFSMA000076 (last data: 2025-10-24 03:00:00)
  ... и еще 11 массивов

Total metrics sent: 675,454,230

Details in log: batch_import_20251127_123919.log
================================================================================
```

**Примеры из реальных запусков:**

| Набор данных | Архивов | Формат | Метрик | Время | Успех |
|--------------|---------|--------|--------|-------|-------|
| VTB HC | 51 | .zip | 88.3B | 14h 54m | 100% |
| Sber Data Collection | 14 | .7z | 675M | 8m 13s | 100% |

---

## 📁 Логирование

Скрипт автоматически создает лог-файл:

```
batch_import_YYYYMMDD_HHMMSS.log
```

Лог-файл содержит:
- Детальную информацию по каждому архиву
- Вывод streaming pipeline
- Ошибки и предупреждения
- Временные метки всех операций

### Просмотр логов

```bash
# Последний лог
ls -t batch_import_*.log | head -1

# Просмотр в реальном времени
tail -f batch_import_20251127_123919.log

# Поиск ошибок
grep -i error batch_import_20251127_123919.log

# Поиск unknown IDs
grep -i unknown batch_import_20251127_123919.log
```

---

## 🔧 Требования

### Python модули

- **Python 3.11+**
- `requests` (для VictoriaMetrics API)
- `py7zr` (для поддержки .7z архивов)
- `zipfile` (встроен)
- `subprocess` (встроен)

### Установка зависимостей

```bash
pip install py7zr requests
```

### Зависимости проекта

Скрипт использует:
- `huawei_streaming_pipeline.py` (в родительской директории)
- `VictoriaMetricsClient` из `/data/projects/Huawei_health_check/victoriametrics_client.py`

### VictoriaMetrics

- Должна быть доступна на `http://localhost:8428` (или указанном URL)
- Должен быть доступен endpoint `/api/v1/import/prometheus`

---

## ⚙️ Параметры командной строки

| Параметр | Тип | Описание | По умолчанию |
|----------|-----|----------|--------------|
| `log_dir` | Позиционный | Директория с архивами (рекурсивный поиск) | `/data/vtb_hc/perf/` |
| `--vm-url` | Опция | URL VictoriaMetrics | `http://localhost:8428` |
| `--skip-existing` | Флаг | Пропускать уже импортированные массивы | `False` |
| `--dry-run` | Флаг | Режим без реального импорта | `False` |
| `--workers` | Опция | Количество workers (зарезервировано) | `None` |

---

## 🔍 Как это работает

### 1. Сканирование директории

Скрипт **рекурсивно** ищет все `.zip` и `.7z` файлы в указанной директории и поддиректориях.

### 2. Для каждого архива:

**a) Извлечение серийного номера**
- Для `.zip`: просмотр содержимого без распаковки, поиск паттерна `PerfData_*_SN_<SERIAL>_SP*`
- Для `.7z`: извлечение SN из имени файла `Data_*_<SN>.7z`
- Fallback: поиск SN в имени родительской директории

**b) Проверка существующих данных** (если `--skip-existing`)
- Запрос к VictoriaMetrics через API
- Проверка метрики `huawei_avg_cpu_usage_percent{SN="..."}` за последние 6 месяцев
- Если данные есть → пропуск

**c) Подготовка к обработке**
- Для `.zip`: прямой запуск pipeline
- Для `.7z`: извлечение Perf ZIP из вложенной структуры во временную директорию

**d) Запуск streaming pipeline**
- Вызов `huawei_streaming_pipeline.py -i <archive.zip> --vm-url <url> --monitor`
- Перехват stdout/stderr в реальном времени
- Извлечение количества отправленных метрик из вывода

**e) Проверка импорта**
- Задержка 2 секунды (для индексации VM)
- Повторный запрос к VictoriaMetrics
- Проверка появления данных

**f) Cleanup**
- Удаление временных файлов (для .7z архивов)

**g) Логирование результата**
- Статус (success/failed/skipped)
- Время обработки
- Количество метрик
- Наличие данных в VM

### 3. Финальный отчет

Генерация детальной статистики по всем обработанным архивам.

---

## 🛠️ Обработка ошибок

### Ошибка в одном архиве

✅ **Скрипт продолжает работу** с остальными архивами
- Логирует ошибку
- Добавляет в список failed imports
- Переходит к следующему архиву

### Недоступность VictoriaMetrics

⚠️ **Предупреждение, но импорт продолжается**
- Проверка данных пропускается
- Импорт выполняется в обычном режиме
- В отчете не будет информации о наличии данных в VM

### Невозможность извлечь серийный номер

⚠️ **Использует fallback имя**
- Присваивает SN = `UNKNOWN_<имя_архива>`
- Продолжает обработку

### Отсутствие py7zr

⚠️ **Предупреждение, .7z файлы пропускаются**
- Выводит предупреждение: "py7zr не установлен! .7z файлы будут пропущены"
- Обрабатывает только .zip файлы

### Прерывание (Ctrl+C)

✅ **Graceful shutdown**
- Завершает текущую операцию
- Генерирует промежуточный отчет
- Сохраняет логи

---

## 💡 Tips & Tricks

### 1. Предварительный анализ

Перед реальным импортом выполните dry-run:

```bash
python3 batch_import.py /data/logs/perf/ --dry-run
```

Это покажет:
- Сколько архивов будет обработано
- Какие серийные номера обнаружены
- Какие массивы уже есть в VM

### 2. Инкрементальный импорт

Для регулярного импорта новых данных:

```bash
python3 batch_import.py /data/logs/perf/ --skip-existing
```

Это автоматически пропустит уже импортированные массивы.

### 3. Мониторинг в реальном времени

Запустите импорт и откройте лог в отдельном терминале:

```bash
# Терминал 1
python3 batch_import.py /data/logs/perf/ --skip-existing

# Терминал 2
tail -f batch_import_$(date +%Y%m%d)*.log
```

### 4. Проверка результатов в VictoriaMetrics

После импорта:

```bash
# Список всех импортированных массивов (за последние 3 месяца)
START=$(date -d '3 months ago' +%s)
curl -s "http://localhost:8428/api/v1/label/SN/values?start=${START}" | jq -r '.data[]' | sort

# Количество массивов
curl -s "http://localhost:8428/api/v1/label/SN/values?start=${START}" | jq -r '.data | length'

# Количество метрик для массива
curl -s "http://localhost:8428/api/v1/series?match[]=huawei_avg_cpu_usage_percent{SN=\"2102352VUV10LB000006\"}" | jq '.data | length'
```

**⚠️ Важно:** Всегда указывайте параметр `start` для запросов к VictoriaMetrics, иначе может вернуться пустой результат!

### 5. Обработка архивов в поддиректориях

Скрипт автоматически находит архивы рекурсивно:

```bash
# Структура директорий:
# /data/logs/perf/
# ├── 10.105.152.20_2102352KRR10KC000013/
# │   └── Data_Dorado6000V3_...7z
# ├── 10.115.55.37_2102352VUVFSMA000076/
# │   └── Data_OceanStorDorado5000V6_...7z
# └── ...

python3 batch_import.py /data/logs/perf/
# ✅ Найдено 14 архивов (из всех поддиректорий)
```

---

## 🔄 Интеграция с CI/CD

### Пример скрипта для cron

```bash
#!/bin/bash
# /usr/local/bin/huawei_batch_import.sh

LOG_DIR="/data/logs/perf/"
VM_URL="http://localhost:8428"

cd /data/projects/monitoring_VM_Grafana

python3 batch_import/batch_import.py "$LOG_DIR" \
    --vm-url "$VM_URL" \
    --skip-existing

# Отправка уведомления при ошибке
if [ $? -ne 0 ]; then
    echo "Batch import failed!" | mail -s "Huawei Import Alert" admin@example.com
fi
```

### Crontab (ежедневно в 2:00)

```cron
0 2 * * * /usr/local/bin/huawei_batch_import.sh >> /var/log/huawei_import.log 2>&1
```

---

## 🐛 Troubleshooting

### Проблема: "VictoriaMetrics is not available"

**Решение:**
```bash
# Проверьте доступность VM
curl http://localhost:8428/health

# Проверьте, запущен ли Docker контейнер
docker ps | grep victoriametrics

# Запустите VM
docker compose up -d victoriametrics
```

### Проблема: "VictoriaMetricsClient not available"

**Решение:**
```bash
# Проверьте путь к клиенту
ls /data/projects/Huawei_health_check/victoriametrics_client.py

# Если файл отсутствует, проверка VM будет пропущена (не критично)
```

### Проблема: "py7zr не установлен"

**Решение:**
```bash
pip install py7zr
```

### Проблема: "Pipeline script not found"

**Решение:**
```bash
# Проверьте наличие streaming pipeline
ls /data/projects/monitoring_VM_Grafana/huawei_streaming_pipeline.py

# batch_import.py должен быть в batch_import/ директории
```

### Проблема: "Perf ZIP файлы не найдены внутри .7z"

**Причина:** Структура .7z архива отличается от ожидаемой

**Решение:**
```bash
# Проверьте содержимое архива
python3 -c "
import py7zr
with py7zr.SevenZipFile('archive.7z', 'r') as z:
    for name in z.getnames():
        if '_Perf_' in name:
            print(name)
"
```

### Проблема: Медленная обработка

**Причины:**
- Большие архивы (>10GB)
- Медленная сеть до VictoriaMetrics
- Недостаточно CPU/RAM

**Решение:**
- Увеличьте ресурсы системы
- Используйте SSD для временных файлов
- Обрабатывайте архивы батчами (разбейте на несколько директорий)

---

## 📈 Performance

### Типичные показатели (проверено на реальных данных)

| Тип архива | Размер | Время обработки |
|------------|--------|-----------------|
| Малый .zip | 200-500MB | ~2-7 минут |
| Средний .zip | 500MB-1GB | ~5-12 минут |
| Большой .zip | 1-3GB | ~10-30 минут |
| Малый .7z | 85-130MB | ~20-40 секунд |
| Средний .7z | 130-250MB | ~40-130 секунд |
| Большой .7z | 500MB+ | ~2-5 минут |

### Throughput

- **~50K-110K метрик/сек** (зависит от CPU и размера файлов)
- **~500MB-2GB RAM** на процесс streaming pipeline
- **~200-300MB RAM** на сам batch_import.py
- **Multiprocessing:** streaming pipeline использует все доступные CPU cores

### Реальные примеры обработки

**Запуск на 51 .zip архиве (VTB):**
- Общее время: 14 часов 54 минуты
- Всего обработано: 88.3 миллиарда метрик
- Средняя скорость: ~1.6 миллиона метрик/минуту
- Throughput итоговый: ~98K метрик/сек

**Запуск на 14 .7z архивах (Sber Data Collection):**
- Общее время: 8 минут 13 секунд
- Всего обработано: 675 миллионов метрик
- Средняя скорость: ~1.4 миллиона метрик/минуту
- 100% успешность (0 ошибок)

---

## 📝 Changelog

### v1.1.0 (2025-11-27)
- ✅ **Поддержка .7z архивов** с вложенной структурой Data Collection
- ✅ **Рекурсивный поиск** архивов в поддиректориях
- ✅ Автоматическое извлечение Perf ZIP из .7z
- ✅ Улучшенное извлечение серийных номеров из имени файла
- ✅ Автоматический cleanup временных файлов
- ✅ **Протестировано на Sber Data Collection:**
  - 14 архивов .7z (85MB - 596MB)
  - 675 миллионов метрик
  - 8 минут 13 секунд
  - 100% успешность (0 ошибок)

### v1.0.0 (2025-11-12/13)
- ✅ Первый релиз
- ✅ Все основные функции реализованы
- ✅ Интеграция с VictoriaMetricsClient
- ✅ Детальное логирование и отчеты
- ✅ Graceful shutdown
- ✅ Dry-run режим
- ✅ **Протестировано на production данных:**
  - 51 архив (195MB - 8.9GB)
  - 88.3 миллиарда метрик
  - 14 часов 54 минуты непрерывной работы
  - 100% успешность (0 ошибок)

---

## 📞 Support

При возникновении проблем:

1. Проверьте логи: `batch_import_*.log`
2. Проверьте streaming pipeline логи: `streaming_pipeline.log`
3. Проверьте доступность VictoriaMetrics: `curl http://localhost:8428/health`
4. Проверьте наличие данных: `curl http://localhost:8428/api/v1/label/SN/values`

---

**Made with ❤️ for Huawei OceanStor Performance Monitoring**
