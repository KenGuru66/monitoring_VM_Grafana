# Huawei Storage Performance Monitoring

**Full-stack web application** для мониторинга и анализа производительности систем хранения данных Huawei OceanStor.

## 🎯 Основные возможности

### 📊 Три режима обработки данных

1. **Parse → Grafana**  
   Потоковая обработка и отправка метрик в VictoriaMetrics для визуализации в Grafana
   - Прямой streaming без промежуточных файлов
   - Поддержка огромных архивов (500GB+, 40+ млрд строк)
   - Параллельная обработка .tgz файлов
   - Минимальное использование памяти

2. **Parse → CSV (Wide Format)**  
   Экспорт данных в широкий CSV формат для анализа в Excel/Python
   - Одна строка = один timestamp
   - Поддержка всех типов ресурсов и метрик
   - Параллельная обработка с автоматическим определением worker'ов

3. **Parse → CSV (Perfmonkey Format)**  
   Экспорт в формат, совместимый с perfmonkey
   - Разделение по типам ресурсов (CPU, Disk, LUN, Pool и т.д.)
   - 8 отдельных CSV файлов
   - Специальные заголовки для каждого типа ресурса

### 🚀 Multi-threaded Compression
- Автоматическое использование до 16 потоков для сжатия CSV файлов
- На системе с 32 vCPU: **~16x ускорение** по сравнению с однопоточным режимом
- Уровень сжатия: 6 (оптимальный баланс скорость/размер)

### 🌐 Web Interface
- **Home Page:** Управление массивами и CSV jobs
  - Просмотр всех массивов в VictoriaMetrics
  - **🆕 Автоматическое отображение Data Collection Interval** для каждого массива
  - **🆕 Автоматический временной диапазон**: Прямые ссылки в Grafana с автоматической передачей:
    - `min_interval` (интервал сбора данных: 5s/1m/5m и т.д.)
    - `from` и `to` (начало и конец периода данных)
    - Не нужно искать данные вручную - Grafana сразу открывается на правильном интервале!
  - Список всех CSV processing jobs
  - Download сгенерированных файлов
  - Удаление данных и файлов
  
- **Upload Page:** Загрузка и обработка архивов
  - Drag & Drop интерфейс (поддержка множества файлов)
  - **🆕 Batch Upload**: Загрузка и обработка множества архивов одновременно
    - **Параллельная загрузка**: Все файлы загружаются одновременно
    - **Последовательная обработка**: Файлы обрабатываются по очереди (конвейер)
    - **Статусы файлов**: ⏳ Pending → ⬆️ Uploading → 📋 Queued → ⚙️ Processing → ✅ Done
    - **Real-time статистика**: Общий прогресс, количество в очереди, завершенные, ошибки
    - **Логи для каждого файла**: Раскрывающаяся секция с детальными логами
  - Поддержка **.zip** и **.7z** архивов
  - Выбор режима обработки (Grafana/CSV Wide/CSV Perfmonkey)
  - Прогресс-бар загрузки и обработки
  - Real-time статус обработки

### 📊 Grafana Dashboards
- **16 pre-configured секций** с 808+ панелями
- **🆕 Smooth Graphs:** Интеллектуальная адаптация интервала запросов
  - Автоматическая передача `$min_interval` из веб-интерфейса
  - Dashboard variable для ручной настройки интервала (5s/15s/30s/1m/5m/15m/30m/1h)
  - Smooth line interpolation для bandwidth метрик
  - Устранение "ступенчатых" графиков при увеличении
- Поддержка всех типов ресурсов:
  - Controller (с NFSV3/V4/V4.1, SMB2/3)
  - **Controller NFSV3 (33 метрики)** 🆕
    - 22 OPS метрики (Operations Per Second)
    - 11 Response Time метрики (Microseconds)
  - Disk, Disk Domain
  - FC Port (47 метрик)
  - ETH_EXP_Port, Ethernet Port, Logical Port
  - Host, LUN, LUN Priority
  - Snapshot LUN (3 метрики)
  - Storage Pool
- **🆕 Analyze - Multi-Resource Comparison** 🎯
  - Интерактивный инструмент для сравнения разных типов ресурсов и метрик на одном графике
  - **4 независимых query** с изолированными настройками
  - **Динамические метрики**: автоматическая загрузка из VictoriaMetrics в зависимости от выбранного ресурса
  - **Умная фильтрация**: метрики соответствуют типу ресурса
    - Controller → CPU, Memory, Bandwidth, IOPS
    - Disk → Utilization, IOPS, Response Time, Bandwidth
    - Host → Response Time, IOPS, Bandwidth
    - LUN → IOPS, Bandwidth, Cache Hit Ratio
    - Controller NFSv3/v4 → NFS OPS, NFS Response Time
  - **Статистическая таблица**: 95th %, Avg, Min, Max для каждой метрики
  - **Multi-select**: выбор нескольких элементов и метрик одновременно
  - **Примеры использования**:
    - Сравнение CPU контроллеров: Controller 0A, 0B, 0C, 0D → CPU usage
    - Full-stack анализ: Controller CPU + Disk Utilization + Host Response Time + LUN IOPS
    - NFS производительность: NFSv3 Read OPS + Write OPS + Read RT + Write RT
    - Производительность дисков: Disk Utilization + IOPS + Bandwidth + Response Time

## 📋 Требования

- **Docker** >= 20.10
- **Docker Compose** >= 2.0
- **Свободное место на диске:** ~20GB для образов Docker
- **RAM:** Минимум 4GB, рекомендуется 8GB+
- **CPU:** Рекомендуется 4+ ядер для параллельной обработки

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd monitoring_VM_Grafana
```

### 2. Настройка переменных окружения

```bash
cp env.example .env
```

Отредактируйте `.env` при необходимости:

```bash
# VictoriaMetrics
VM_URL=http://victoriametrics:8428
VM_IMPORT_URL=http://victoriametrics:8428/api/v1/import/prometheus

# Grafana
GRAFANA_URL=http://localhost:3000

# API Settings
MAX_UPLOAD_SIZE=10737418240  # 10GB
JOB_TIMEOUT=86400            # 24 hours
JOB_TTL_HOURS=24             # Auto-cleanup after 24 hours

# Web UI - ВАЖНО для WSL/Docker Desktop!
# Замените localhost на IP вашей WSL машины
VITE_API_URL=http://localhost:8000      # или http://<WSL_IP>:8000
VITE_GRAFANA_URL=http://localhost:3000  # или http://<WSL_IP>:3000
```

### 3. Запуск приложения

**Два режима запуска:**

#### 🖥️ Локальная разработка (WSL / Docker Desktop)

```bash
docker compose up -d
```

Использует простые Docker volumes — данные хранятся внутри Docker.

#### 🏭 Production сервер (Linux с bind mounts)

```bash
# Создайте директории на хосте
sudo mkdir -p /data/vmdata /data/grafana /data/jobs
sudo chown -R $(id -u):$(id -g) /data/vmdata /data/grafana /data/jobs

# Запуск с production конфигурацией
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
```

Данные хранятся в `/data/` на хост-системе для persistence.

### Доступ к сервисам

- **Web UI:** http://localhost:3001
- **API:** http://localhost:8000
- **Grafana:** http://localhost:3000 (admin/changeme)
- **VictoriaMetrics:** http://localhost:8428

> **⚠️ WSL/Docker Desktop:** Если Web UI не может загрузить файлы, используйте IP адрес WSL вместо localhost:
> ```bash
> # Узнать IP WSL
> hostname -I | awk '{print $1}'
> # Обновить .env и пересобрать: docker compose build web && docker compose up -d web
> ```

### 4. Первая загрузка данных

1. Откройте http://localhost:3001
2. Перейдите на вкладку **Upload**
3. Загрузите ZIP архив с файлами производительности Huawei
4. Выберите режим обработки:
   - **Parse → Grafana** - для мониторинга в реальном времени
   - **Parse → CSV (Wide)** - для экспорта в широком формате
   - **Parse → CSV (Perfmonkey)** - для экспорт в perfmonkey формате

## 🔄 Batch Import - Массовая загрузка логов

Для **массового импорта множества архивов** используйте `batch_import.py` - скрипт для последовательной обработки всех ZIP файлов в директории.

### Базовое использование

```bash
# Импорт всех архивов из директории
python3 batch_import/batch_import.py /data/vtb_hc/perf/

# С пропуском уже импортированных массивов
python3 batch_import/batch_import.py /data/vtb_hc/perf/ --skip-existing

# Dry-run режим (тестовый прогон без импорта)
python3 batch_import/batch_import.py /data/vtb_hc/perf/ --dry-run
```

### Ключевые возможности

- ✅ Автоматическое обнаружение всех ZIP архивов
- ✅ Извлечение серийных номеров без полной распаковки
- ✅ Проверка наличия данных в VictoriaMetrics (пропуск дубликатов)
- ✅ Real-time логирование процесса
- ✅ Детальная статистика и финальный отчет
- ✅ Graceful shutdown (Ctrl+C) с сохранением прогресса
- ✅ Продолжение работы при ошибках в отдельных архивах

### Пример финального отчета

```
================================================================================
📊 BATCH IMPORT SUMMARY
================================================================================
Total archives processed: 51/51
  - Successful imports:   48
  - Failed imports:       2
  - Skipped:              1

Total time: 3h 25m
Average time per archive: 4m 1s

Arrays with data in VictoriaMetrics: 48
  - 2102352VUV10LB000006 (last data: 2025-10-16)
  - 2102352VUV10LB000008 (last data: 2025-10-15)
  ...

Details in log: batch_import_20251112_143025.log
================================================================================
```

📖 **Полная документация:** [batch_import/BATCH_IMPORT_README.md](batch_import/BATCH_IMPORT_README.md)

## 📁 Структура проекта

```
monitoring_VM_Grafana/
├── api/                          # FastAPI backend
│   ├── main.py                   # API endpoints
│   ├── requirements.txt          # Python dependencies
│   └── Dockerfile
├── web/                          # React frontend
│   ├── src/
│   │   ├── App.tsx              # Main app component
│   │   ├── Home.tsx             # Home page (arrays & CSV jobs)
│   │   ├── Upload.tsx           # Upload page
│   │   └── App.css              # Styles
│   ├── package.json
│   └── Dockerfile
├── parsers/                      # Парсеры и словари
│   ├── streaming_pipeline.py    # Streaming → VictoriaMetrics
│   ├── csv_wide_parser.py       # CSV wide format
│   └── dictionaries/            # Словари метрик и ресурсов
│       ├── METRIC_DICT.py       # 743+ метрики (названия)
│       ├── RESOURCE_DICT.py     # 51+ тип ресурсов
│       └── METRIC_CONVERSION.py # 49 метрик с конверсией единиц
├── perfmonkey/                   # Perfmonkey format parser
│   └── perf_zip2csv_wide.py
├── grafana/                      # Grafana provisioning
│   ├── dashboards/
│   └── datasources/
├── batch_import/                 # Batch import инструмент 🆕
│   ├── batch_import.py          # Скрипт массового импорта
│   └── BATCH_IMPORT_README.md   # Документация
├── huawei_streaming_pipeline.py  # Streaming to VictoriaMetrics
├── docker-compose.yml            # Docker orchestration
└── README.md                     # This file
```

## 🔧 Архитектура

### Компоненты

1. **Web (Nginx + React)**
   - Single Page Application
   - Responsive UI с поддержкой drag & drop
   - Real-time статус обработки

2. **API (FastAPI + Python 3.11)**
   - RESTful API endpoints
   - Background task processing с ThreadPoolExecutor
   - Multi-threaded CSV compression
   - Automatic job cleanup (24h TTL)

3. **VictoriaMetrics**
   - Time-series database для метрик
   - High-performance хранилище
   - Совместимость с Prometheus API

4. **Grafana**
   - Визуализация метрик
   - Pre-configured dashboards
   - Data source автоматически настроен

### Workflow обработки

#### Режим Grafana:
```
ZIP Upload → Extract .tgz → Parse Binary → Stream to VictoriaMetrics → Visualize in Grafana
```

#### Режим CSV:
```
ZIP Upload → Extract .tgz → Parse Binary → Generate CSV → Multi-threaded Compression → Download
```

## 📊 API Endpoints

### Upload & Processing

```bash
# Upload archive and start processing
POST /api/upload
  - file: ZIP archive (multipart/form-data)
  - target: "grafana" | "csv" | "perfmonkey"

# Get processing status
GET /api/status/{job_id}

# Get list of all jobs
GET /api/jobs
```

### Arrays Management (VictoriaMetrics)

```bash
# List all arrays with metadata (includes scrape_interval)
GET /api/arrays
# Response example:
# {
#   "arrays": ["SN1", "SN2"],
#   "arrays_metadata": [
#     {"sn": "SN1", "scrape_interval": "1m"},
#     {"sn": "SN2", "scrape_interval": "5m"}
#   ],
#   "total": 2
# }

# Delete specific array
DELETE /api/arrays/{serial_number}

# Delete all arrays
DELETE /api/arrays
```

### CSV Files Management

```bash
# List all CSV processing jobs
GET /api/csv-jobs

# Get files for specific job
GET /api/files/{job_id}

# Download specific file
GET /api/file/{job_id}/{filename}

# Delete all files for job
DELETE /api/files/{job_id}
```

## 🛠️ Управление

### Просмотр логов

```bash
# Все сервисы
docker compose logs -f

# Конкретный сервис
docker compose logs -f api
docker compose logs -f web
docker compose logs -f victoriametrics
docker compose logs -f grafana
```

### Остановка и запуск

```bash
# Остановка
docker compose down

# Запуск
docker compose up -d

# Перезапуск
docker compose restart

# Пересборка после изменений
docker compose build
docker compose up -d
```

### Очистка данных

```bash
# Удалить все данные (включая volumes)
docker compose down -v

# Удалить только VictoriaMetrics данные
docker volume rm monitoring_vm_grafana_vm_data

# Удалить только CSV job файлы
docker volume rm monitoring_vm_grafana_jobs_data
```

### Обновление Grafana dashboards

```bash
# Dashboards находятся в
./grafana/dashboards/

# После изменений:
docker compose restart grafana
```

## 🔍 Мониторинг и отладка

### Проверка здоровья сервисов

```bash
# API health check
curl http://localhost:8000/health

# VictoriaMetrics
curl http://localhost:8428/-/healthy

# Список метрик в VM
curl http://localhost:8428/api/v1/label/SN/values
```

### Типичные проблемы

**1. Ошибка "File too large"**
```bash
# Увеличьте MAX_UPLOAD_SIZE в .env
MAX_UPLOAD_SIZE=21474836480  # 20GB
docker compose restart api
```

**2. Нет данных в Grafana**
```bash
# Проверьте наличие метрик в VictoriaMetrics
curl "http://localhost:8428/api/v1/label/SN/values?start=1577836800"

# Проверьте логи streaming pipeline
docker compose logs api | grep "STREAMING PIPELINE"
```

**3. CSV файлы не генерируются**
```bash
# Проверьте логи парсера
docker compose logs api | grep "Compressing"

# Проверьте наличие файлов
docker exec monitoring_vm_grafana-api-1 ls -lah /app/jobs/
```

## 🔄 Metric Unit Conversions

Проект автоматически конвертирует единицы измерения метрик, когда сырые данные в `.dat` файлах имеют другие единицы, чем указано в названии метрики.

### METRIC_CONVERSION.py

Централизованный словарь конверсий находится в `parsers/dictionaries/METRIC_CONVERSION.py`:

**Bandwidth метрики** (KB/s → MB/s, делим на 1024):
- `311`, `312`, `313`: Throughput, Read/Write throughput
- `511`, `512`: File bandwidth, Throughput

**Latency метрики** (us → ms, делим на 1000):
- `369-385`: Service Time, I/O Response Time
- `429`, `472`, `509`, `510`: Max/Min Latency For Operations
- `508`: Average Latency For Operations
- `523-525`: Service time, Read/Write OPS Response Time
- `745-807`: NFS/CIFS I/O Response Time, Back-End Response Time
- `1139-1319`: Link Transmission Latency, Command Response Time
- `30013`, `30015`: NFS OPS Response Time

**Всего: 49 метрик с автоматической конверсией**

### Просмотр статистики конверсий

```bash
cd parsers/dictionaries
python3 METRIC_CONVERSION.py
```

Выведет список всех метрик с конверсией, сгруппированных по типам.

### Добавление новых конверсий

Для добавления новой метрики с конверсией:

1. Откройте `parsers/dictionaries/METRIC_CONVERSION.py`
2. Добавьте строку в соответствующую секцию:
   ```python
   "metric_id": коэффициент,  # Название метрики - комментарий
   ```
3. Сохраните файл - оба парсера автоматически используют обновленный словарь

## 🎯 Intelligent Graph Rendering

Проект автоматически определяет интервал сбора данных (scrape_interval) и временной диапазон для каждого массива, обеспечивая корректное отображение графиков в Grafana без ручной настройки.

### Как это работает

1. **При парсинге**: 
   - Скрипт `huawei_streaming_pipeline.py` извлекает интервал из заголовка `.dat` файла
   - Добавляет label `scrape_interval="60"` (в секундах) к каждой метрике
   - Отправляет в VictoriaMetrics с правильными timestamps

2. **В веб-интерфейсе**:
   - API запрашивает `scrape_interval` для каждого массива
   - API определяет первый и последний timestamp данных (временной диапазон)
   - Конвертирует интервал в читаемый формат (60s → 1m, 300s → 5m)
   - Конвертирует timestamps в миллисекунды для Grafana
   - Отображает badge "⏱️ Data Collection Interval: 1m" рядом с SN

3. **В Grafana**:
   - При клике "Open in Grafana" автоматически добавляется:
     - `&var-min_interval=1m` (правильный интервал для smooth графиков)
     - `&from=1760911200000&to=1761044400000` (начало и конец данных в milliseconds)
   - Dashboard использует `${min_interval}` для установки правильного step в queries
   - Grafana сразу открывается на правильном временном диапазоне
   - Графики отображаются плавно без "ступенек"
   - **Не нужно искать данные вручную!**

### Ручная настройка интервала

В Grafana dashboard вверху есть dropdown `min_interval` с опциями:
- `5s`, `15s`, `30s` - для высокочастотных данных
- `1m`, `5m`, `15m`, `30m`, `1h` - для стандартных интервалов

Выберите интервал, соответствующий вашим данным, если автоматическое определение не сработало.

### Troubleshooting "ступенчатых" графиков

Если графики выглядят "ступенчато" при увеличении:

1. **Проверьте scrape_interval**:
   ```bash
   curl "http://localhost:8428/api/v1/query_range?query=huawei_read_bandwidth_mb_s{SN=\"YOUR_SN\"}&start=$(date -d '7 days ago' +%s)&end=$(date +%s)&step=1d" | jq '.data.result[0].metric.scrape_interval'
   ```

2. **Установите правильный min_interval** в Grafana dashboard

3. **Для старых данных** (без scrape_interval label):
   - Удалите массив через веб-интерфейс
   - Перепарсите данные с обновленным `huawei_streaming_pipeline.py`

## 📈 Performance Tips

### Оптимизация для больших файлов

1. **Увеличьте количество workers** для парсинга:
   - CSV Wide: автоматически определяется (cpu_count - 2)
   - Perfmonkey: `--workers 30` в `api/main.py`

2. **Используйте SSD** для Docker volumes

3. **Настройте VictoriaMetrics** для больших данных:
```yaml
# в docker-compose.yml
victoriametrics:
  command:
    - --retentionPeriod=12  # месяцев
    - --maxLabelsPerTimeseries=50
    - --memory.allowedPercent=80
```

## 🔐 Безопасность

- Измените дефолтные пароли Grafana
- Используйте reverse proxy (nginx) с HTTPS в production
- Ограничьте доступ к портам через firewall
- Регулярно обновляйте Docker образы

## 📝 Changelog

### v2.6.0 (Current) - December 3, 2025
- ✅ **Batch Upload в Web UI**: Загрузка и обработка множества архивов через веб-интерфейс
  - **Параллельная загрузка**: Все выбранные файлы загружаются одновременно
  - **Конвейерная обработка**: Файлы обрабатываются последовательно (кто первый загрузился - первый обрабатывается)
  - **Pipeline статусы**: 
    - ⏳ Pending (ожидает)
    - ⬆️ Uploading (загружается)
    - 📋 Queued (в очереди на обработку)
    - ⚙️ Processing (обрабатывается)
    - ✅ Done (готово)
    - ❌ Error (ошибка)
  - **Real-time статистика**: Отображение количества файлов в каждом статусе
  - **Детальные логи**: Раскрывающаяся секция с логами для каждого файла
  - **Итоговая сводка**: После завершения всех файлов показывается общая статистика
  
- ✅ **Поддержка .7z архивов в Web UI**:
  - Автоматическое извлечение Perf ZIP из .7z архивов
  - Структура: `DataCollect/History_Performance_Data/<IP>/(*_Perf_*.zip)`
  - Прозрачная обработка - пользователю не нужно распаковывать вручную
  
- ✅ **Улучшения UI**:
  - Новые иконки для статусов (📋 для queued, ⬆️ для uploading)
  - Цветовая индикация статусов в статистике
  - Визуальное выделение файлов в очереди на обработку

### v2.5.0 - November 27, 2025
- ✅ **Универсальный Docker Compose**: Поддержка локальной разработки и production
  - `docker-compose.yml` — универсальная конфигурация с простыми named volumes (работает везде: WSL, Docker Desktop, Linux)
  - `docker-compose.prod.yml` — production конфигурация с bind mounts к `/data/` для persistent storage
  - Команда для production: `docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d`
  
- ✅ **Исправлена совместимость с WSL/Docker Desktop**:
  - Убраны `driver_opts` из volumes (не поддерживаются в Docker Desktop)
  - Добавлен `PYTHONPATH=/app` в Dockerfile для корректного импорта модулей
  - Документация по использованию IP адреса WSL вместо localhost
  
- ✅ **Улучшения документации**:
  - Добавлена инструкция по запуску в двух режимах (локальный/production)
  - Добавлены примечания для WSL пользователей

### v2.4.0 - October 30, 2025
- ✅ **Analyze - Multi-Resource Comparison**: Новый интерактивный инструмент для сравнительного анализа
  - **4 независимых query** на одном графике с полной изоляцией
  - **Динамическая загрузка метрик**: Автоматическое получение списка метрик из VictoriaMetrics в зависимости от выбранного типа ресурса
  - **Умная фильтрация**: Метрики соответствуют типу ресурса (Controller → CPU/Memory, Disk → Utilization, Host → Response Time, и т.д.)
  - **12 новых переменных**: Resource_Q1-Q4, Element_Q1-Q4, Metric_Q1-Q4 для гибкой настройки
  - **Статистическая таблица**: 95th %, Avg, Min, Max для каждой метрики
  - **Multi-select**: Возможность выбора нескольких элементов и метрик одновременно
  - **Примеры use-cases**:
    - Full-stack анализ: Controller CPU + Disk Utilization + Host Response Time + LUN IOPS
    - Сравнение контроллеров: CPU usage для 4 контроллеров одновременно
    - NFS производительность: Read/Write OPS + Read/Write Response Time
    - Производительность дисков: Utilization + IOPS + Bandwidth + Response Time

- ✅ **Grafana Dashboard Enhancements**:
  - **Уникальные иконки для групп**: Каждая из 17 групп dashboard теперь имеет уникальную осмысленную иконку (🎛️ Controller, 💿 Disk, 🖥️ Host, 💾 LUN, 🏊 Storage Pool, и т.д.)
  - **Статистические таблицы**: Все 862 графика теперь показывают таблицу со статистикой (95th %, Avg, Min, Max) внизу
  - **NFSv3 Nomenclature**: Все NFSv3 панели (33 шт.) обновлены с официальной номенклатурой Huawei
    - OPS метрики: "NFS V3 LOOKUP OPS" → "Total NFS lookup OPS (Number/s)"
    - RT метрики: "NFS V3 LOOKUP Response Time" → "NFS lookup response time(us)"

- ✅ **Bug Fixes**:
  - Исправлена логика предупреждений об unknown IDs в парсерах
  - Улучшена изоляция queries в группе Analyze
  - Исправлены datasource UID для ETH Expansion Port панелей

### v2.3.0 - October 27, 2025
- ✅ **NFSv3 Metrics Support**: Добавлена полная поддержка 33 NFSv3 метрик
  - **22 OPS метрики**: LOOKUP, GETATTR, REMOVE, CREATE, ACCESS, READ, WRITE, MKDIR, READLINK, READDIR, READDIRPLUS, FSSTAT, FSINFO, PATHCONF, COMMIT, и другие
  - **11 Response Time метрики**: LOOKUP RT, PATHCONF RT, READDIR RT, GETATTR RT, MKDIR RT, ACCESS RT, READDIRPLUS RT, и другие
  - **Полная диагностика**: 36.8M метрик загружено, 81 NFSv3 метрика в VictoriaMetrics
  - **Dashboard исправления**: Все queries обновлены с regex match (`SN=~"$SN"`) для корректной работы
  
- ✅ **Автоматическое обновление словарей**:
  - Новый инструмент `auto_update_dictionaries.py`
  - Автоматическое сканирование логов парсинга на unknown IDs
  - Автоматическое добавление новых ресурсов и метрик в `METRIC_DICT.py` и `RESOURCE_DICT.py`
  - Метаданные: дата добавления, комментарии "требует уточнения"
  - Интеграция с `huawei_streaming_pipeline.py` - автозапуск после парсинга
  
- ✅ **Документация**:
  - `NFSV3_TROUBLESHOOTING_REPORT.md` - полный отчёт по диагностике NFSv3
  - Пошаговая инструкция по траблшутингу "No data" проблем
  - Примеры API запросов к VictoriaMetrics
  - Полезные команды для проверки данных

### v2.2.0 - October 22, 2025
- ✅ **Intelligent Graph Rendering**: Автоматическая адаптация интервала запросов Grafana
  - **scrape_interval label**: Автоматическое добавление интервала сбора данных (5s/15s/30s/1m/5m и т.д.) к каждой метрике при парсинге
  - **Web UI Enhancement**: Отображение "⏱️ Data Collection Interval" badge рядом с каждым массивом
  - **Auto min_interval**: Автоматическая передача правильного `$min_interval` в URL Grafana при переходе
  - **🆕 Auto Time Range**: Автоматическое определение временного диапазона данных
    - API извлекает первый и последний timestamp из VictoriaMetrics
    - Автоматическая передача `from` и `to` параметров в URL Grafana (в миллисекундах)
    - Grafana сразу открывается на правильном временном интервале
    - Не нужно искать данные вручную!
  - **Dashboard Variable**: Добавлена переменная `$min_interval` в Grafana для ручной настройки (5s/15s/30s/1m/5m/15m/30m/1h)
  - **Smooth Graphs**: Исправлена проблема "ступенчатых" графиков при увеличении
    - Smooth line interpolation для bandwidth метрик
    - `spanNulls=true` для корректного отображения пропущенных значений
  
- ✅ **API Improvements**:
  - Исправлен `/api/arrays` endpoint для корректного извлечения `scrape_interval` из VictoriaMetrics
  - Добавлено извлечение временного диапазона данных (`time_from`, `time_to`)
  - Использование `series` API для надежного получения labels и `query_range` для временных диапазонов
  - Автоматическая конверсия секунд в читаемый формат (60s → 1m, 300s → 5m)
  - Конверсия timestamps в миллисекунды для Grafana (Unix epoch * 1000)
  
- ✅ **Grafana Dashboard Updates**:
  - Все timeseries панели (775+) теперь используют `${min_interval}` для корректного step
  - Фиксация верхней планки 100% для всех "usage" метрик (13 панелей)
  - Исправлены units для latency метрик (ms вместо percent)

### v2.1.1 - October 21, 2025
- ✅ **METRIC_CONVERSION.py**: Централизованная система конверсии единиц измерения
  - Создан новый модуль `parsers/dictionaries/METRIC_CONVERSION.py`
  - 49 метрик с автоматической конверсией единиц
  - DRY принцип: единый источник истины для обоих парсеров
  - Встроенная документация и статистика
  
- ✅ **Исправлены критические проблемы с метриками**:
  - **Bandwidth метрики**: File bandwidth, Throughput (KB/s → MB/s, ÷1024)
    - Исправлено 5 метрик: 311, 312, 313, 511, 512
    - Было: TB/s (в 1024 раза больше) ❌ → Стало: GB/s ✅
  
  - **Latency метрики**: Average/Max/Min Latency (us → ms, ÷1000)
    - Исправлено 44 метрики: 369-385, 429, 472, 508-510, 523-525, 745-807, и др.
    - Было: огромные значения в микросекундах ❌ → Стало: миллисекунды ✅
  
- ✅ **Grafana Dashboard исправления**:
  - Исправлен unit для 12 панелей (percent → ms)
  - Average Latency For Operations: 4 панели (NFSV3/V4/V4.1/SMB)
  - Max/Min Latency For Operations: 8 панелей
  - Теперь все latency метрики корректно отображаются в миллисекундах
  
- ✅ **Рефакторинг кода**:
  - Устранено дублирование: ~110 строк кода в двух парсерах
  - Улучшена поддерживаемость: изменения в одном месте
  - Добавлена возможность запуска `python3 METRIC_CONVERSION.py` для статистики

### v2.1.0 - October 16, 2025
- ✅ **Grafana Dashboards Update**: Добавлены недостающие секции
  - FC Port section (47 метрик)
  - Snapshot LUN section (3 метрики)
  - Всего: 16 секций, 775+ панелей
- ✅ **Streaming Pipeline Verification**: Полная проверка соответствия CSV
  - 100% совпадение метрик и ресурсов
  - 2.4M точек данных обработано за 2.1 сек
  - Throughput: 1.1M метрик/сек

### v2.0.0
- ✅ Multi-mode processing (Grafana/CSV Wide/CSV Perfmonkey)
- ✅ Multi-threaded CSV compression (16 threads)
- ✅ New Home page with arrays & CSV jobs management
- ✅ Improved UI/UX with progress tracking
- ✅ Automatic job cleanup (24h TTL)
- ✅ HTTP Range support for large file downloads
- ✅ Real-time file list polling for CSV jobs

### v1.0.0
- Initial release with Grafana integration

## 📞 Support

При возникновении проблем:
1. Проверьте логи: `docker compose logs -f`
2. Убедитесь, что все порты свободны: `8000, 8080, 3000, 8428`
3. Проверьте свободное место на диске: `df -h`

## 📄 License

Proprietary - Internal Use Only

---

**Made with ❤️ for Huawei OceanStor Performance Analysis**
