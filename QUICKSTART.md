# Quick Start Guide

Минимальный набор команд для запуска системы мониторинга Huawei Storage Performance.

## ⚡ 3-минутный старт

### 1. Подготовка

```bash
# Клонирование
git clone https://github.com/KenGuru66/monitoring_VM_Grafana.git
cd monitoring_VM_Grafana

# Конфигурация
cp env.example .env
```

### 2. Запуск

```bash
# Запуск всех сервисов
docker compose up -d

# Проверка статуса
docker compose ps
```

### 3. Использование

Откройте в браузере: **http://localhost:8080**

#### Режим Grafana (мониторинг)
1. Перейдите на **Upload**
2. Загрузите ZIP архив
3. Выберите **Parse → Grafana**
4. Дождитесь завершения (100%)
5. Нажмите **Open in Grafana**

#### Режим CSV (экспорт данных)
1. Перейдите на **Upload**
2. Загрузите ZIP архив
3. Выберите **Parse → CSV (Wide)** или **Parse → CSV (Perfmonkey)**
4. Дождитесь завершения
5. Скачайте сгенерированные `.csv.gz` файлы

## 🔧 Управление

### Просмотр логов
```bash
docker compose logs -f
```

### Перезапуск
```bash
docker compose restart
```

### Остановка
```bash
docker compose down
```

### Полная очистка (включая данные)
```bash
docker compose down -v
```

## 🌐 Доступ к сервисам

| Сервис | URL | Credentials |
|--------|-----|-------------|
| Web UI | http://localhost:8080 | - |
| API | http://localhost:8000 | - |
| Grafana | http://localhost:3000 | admin / admin |
| VictoriaMetrics | http://localhost:8428 | - |

## 🐛 Troubleshooting

### Порты заняты
```bash
# Проверка
sudo netstat -tulpn | grep -E '8000|8080|3000|8428'

# Изменение портов в .env
nano .env
```

### Нет места на диске
```bash
# Проверка
df -h

# Очистка
docker system prune -a --volumes -f
```

### Контейнеры не запускаются
```bash
# Просмотр логов
docker compose logs

# Пересборка
docker compose build --no-cache
docker compose up -d
```

## 📖 Полная документация

- **README.md** - Основная документация
- **DEPLOYMENT.md** - Руководство по развертыванию
- **SCRIPTS.md** - Описание скриптов
- **PROJECT_STRUCTURE.md** - Структура проекта

## 💡 Полезные команды

```bash
# Просмотр всех массивов в VictoriaMetrics
curl "http://localhost:8428/api/v1/label/SN/values?start=1577836800" | jq

# Статус обработки job
curl http://localhost:8000/api/status/{job_id} | jq

# Список CSV jobs
curl http://localhost:8000/api/csv-jobs | jq

# Health check
curl http://localhost:8000/health
curl http://localhost:8428/-/healthy
```

---

**🎉 Готово! Система запущена и готова к работе.**

Для более детальной информации см. [README.md](README.md)

