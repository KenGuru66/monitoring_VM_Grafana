# 🚀 Quick Start Guide - Web UI

Простая инструкция для запуска веб-интерфейса Huawei Storage Monitoring.

## Предварительные требования

- Docker и Docker Compose установлены
- Порты 3000, 3001, 8000, 8428 свободны
- Минимум 4GB свободной RAM

## Запуск за 3 шага

### 1. Клонируйте проект (если еще не сделано)

```bash
cd /data/projects/monitoring_VM_Grafana
```

### 2. Запустите все сервисы

```bash
docker-compose up -d --build
```

**Процесс сборки займет 2-5 минут при первом запуске.**

### 3. Откройте веб-интерфейс

Перейдите в браузере: **http://localhost:3001**

---

## ✅ Проверка статуса

```bash
# Проверить все сервисы
docker-compose ps

# Должны быть запущены:
# - victoriametrics
# - grafana
# - api
# - web
```

**Ожидаемый вывод:**

```
NAME                  STATUS    PORTS
victoriametrics       Up        0.0.0.0:8428->8428/tcp
grafana               Up        0.0.0.0:3000->3000/tcp
api                   Up        0.0.0.0:8000->8000/tcp
web                   Up        0.0.0.0:3001->80/tcp
```

---

## 📊 Доступные интерфейсы

| Сервис | URL | Описание |
|--------|-----|----------|
| 🌐 Web UI | http://localhost:3001 | Загрузка и обработка логов |
| 📈 Grafana | http://localhost:3000 | Визуализация метрик (admin/changeme) |
| 🔌 API Docs | http://localhost:8000/docs | Swagger документация API |
| 🗄️ VictoriaMetrics | http://localhost:8428 | TSDB эндпоинт |

---

## 🎯 Как использовать

### Загрузка логов через Web UI

1. **Откройте** http://localhost:3001

2. **Перетащите** .zip архив с логами в область загрузки
   - Или нажмите "Browse Files" и выберите файл
   - Максимальный размер: 10GB

3. **Нажмите** "Run Parsing"
   - Система автоматически:
     - Распакует архив
     - Извлечет серийные номера массивов
     - Распарсит .tgz файлы в метрики
     - Импортирует в VictoriaMetrics

4. **Наблюдайте** прогресс
   - Прогресс-бар показывает 0-100%
   - Статус обновляется каждые 2 секунды

5. **Откройте Grafana**
   - После завершения нажмите "Open in Grafana"
   - Просмотрите метрики производительности

---

## 🐛 Решение проблем

### Порты заняты

```bash
# Проверить занятые порты
sudo netstat -tulpn | grep -E '3000|3001|8000|8428'

# Изменить порты в docker-compose.yml или .env
```

### Сервисы не запускаются

```bash
# Посмотреть логи
docker-compose logs api
docker-compose logs web

# Перезапустить
docker-compose restart
```

### Ошибка сборки

```bash
# Очистить кеш и пересобрать
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

### API не доступен из Web UI

```bash
# Проверить, что API запущен
curl http://localhost:8000/health

# Проверить переменные окружения
docker-compose config | grep VITE_API_URL
```

---

## 🛑 Остановка сервисов

```bash
# Остановить все сервисы (сохранить данные)
docker-compose stop

# Остановить и удалить контейнеры (сохранить данные)
docker-compose down

# Удалить всё включая данные (⚠️ Осторожно!)
docker-compose down -v
```

---

## 📝 Следующие шаги

1. **Настройка Grafana дашбордов**
   - Логин: admin / changeme (измените пароль!)
   - Создайте custom дашборды для ваших метрик

2. **API интеграция**
   - Изучите API: http://localhost:8000/docs
   - Автоматизируйте загрузку через curl или Python

3. **Производство**
   - Настройте переменные окружения в `.env`
   - Используйте reverse proxy (nginx/traefik)
   - Настройте SSL/TLS
   - Ограничьте CORS в API

---

## 🔗 Полезные ссылки

- [Полная документация](README.md)
- [VictoriaMetrics Docs](https://docs.victoriametrics.com/)
- [Grafana Docs](https://grafana.com/docs/)
- [FastAPI Docs](https://fastapi.tiangolo.com/)

---

**Приятной работы! 🎉**

