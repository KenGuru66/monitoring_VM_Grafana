# ⚡ Quick Start - CSV Mode Fix

## 🎯 Проблема

UI показывает кнопку "Open in Grafana" для CSV режимов → **ИСПРАВЛЕНО**

## ✅ Решение (одна команда)

```bash
cd /data/projects/monitoring_VM_Grafana && ./rebuild.sh && echo "✅ Готово! Очистите кэш браузера (Ctrl+Shift+R)"
```

## 📝 Или пошагово:

```bash
# 1. Перейти в директорию
cd /data/projects/monitoring_VM_Grafana

# 2. Пересобрать
./rebuild.sh

# 3. Очистить кэш браузера
# Chrome/Firefox: Ctrl+Shift+R
# Safari: Cmd+Option+R

# 4. Тест (опционально)
./test_csv_mode.sh test.zip
```

## 🧪 Проверка

1. Открыть: `http://localhost:3001`
2. Загрузить ZIP
3. Выбрать: **"Parse → CSV (Wide)"**
4. Проверить:
   - ❌ НЕТ кнопки Grafana
   - ✅ ЕСТЬ таблица файлов
   - ✅ Кнопки Download работают

## 🔍 Быстрая диагностика

```bash
# Статус
docker-compose ps

# Логи (если что-то не так)
docker-compose logs api | tail -50

# Здоровье API
curl http://localhost:8000/health

# Список job'ов
curl http://localhost:8000/api/jobs
```

## 📚 Полная документация

- `README_FIX.md` - Детальная инструкция
- `REBUILD_GUIDE.md` - Гайд по пересборке и тестам
- `CHANGELOG.md` - Что изменилось

---

**Время:** 5 минут | **Сложность:** ⭐☆☆☆☆

Вопросы? → Смотрите `FIX_CSV_UI.md` (раздел Troubleshooting)
