# 🎉 Обновления системы мониторинга

## ✅ Исправления и улучшения (6 октября 2025)

### 1. 🔗 Правильный URL дашборда Grafana

**Было:**
```
http://localhost:3000/d/huawei-storage/huawei-storage-performance
```

**Стало:**
```
http://localhost:3000/d/huawei-oceanstor-real/huawei-oceanstor-real-data
```

**Изменено в:** `api/main.py` (строка 172)

---

### 2. 🌐 Поддержка реального IP адреса сервера

Теперь можно указать реальный IP адрес сервера вместо localhost для удалённого доступа.

**Как настроить:**

Создайте файл `.env` в корне проекта:

```bash
# Замените на IP вашего сервера
GRAFANA_URL=http://192.168.1.100:3000
VITE_API_URL=http://192.168.1.100:8000
VITE_GRAFANA_URL=http://192.168.1.100:3000
```

Затем пересоберите контейнеры:

```bash
docker compose down
docker compose up -d --build
```

**Пример для production:**

```bash
# .env
GRAFANA_URL=http://monitoring-server.company.com:3000
VITE_API_URL=http://monitoring-server.company.com:8000
VITE_GRAFANA_URL=http://monitoring-server.company.com:3000
```

---

### 3. 📊 Парсинг всех метрик по умолчанию

Теперь при обработке логов **автоматически используется флаг `--all-metrics`**, что означает:

- ✅ Парсятся ВСЕ метрики из METRIC_DICT и RESOURCE_DICT
- ✅ Более полная картина производительности
- ✅ Все доступные графики в Grafana

**Изменено в:** `api/main.py` (строка 125)

**Что это значит:**
- Раньше: парсились только DEFAULT метрики (~30 метрик)
- Теперь: парсятся ВСЕ метрики (100+ метрик)

---

### 4. ⏱️ Таймаут для задач

Добавлена защита от зависших задач:

**Параметры:**
- **Таймаут по умолчанию:** 1 час (3600 секунд)
- **Настройка:** через переменную `JOB_TIMEOUT` в `.env`

**Пример настройки:**

```bash
# .env
JOB_TIMEOUT=7200  # 2 часа для больших файлов
```

**Что происходит при таймауте:**
- ❌ Процесс обработки принудительно завершается
- 📝 Статус задачи меняется на "error"
- 🧹 Загруженный файл удаляется
- 📋 Логируется ошибка таймаута

**Изменено в:** `api/main.py` (строки 62, 156-160, 209-226)

---

### 5. 📈 Прогресс-бар загрузки файла

**Новые возможности:**

✅ **Real-time прогресс загрузки**
- Показывает скорость загрузки (MB/s)
- Отображает размер загруженных данных
- Обновляется каждую секунду

✅ **Статус "uploading"**
- Отдельный статус для процесса загрузки
- Иконка загрузки с анимацией
- Информация о скорости и размере

**Пример отображения:**

```
📊 Upload Speed: 45.32 MB/s
📁 Uploaded: 523.45 MB
```

**Изменено в:**
- Backend: `api/main.py` (строки 271-380)
- Frontend: `web/src/App.tsx` (строки 8-18, 131-174, 275-282)
- Стили: `web/src/App.css` (строки 251-264)

---

## 📊 Технические детали

### Новые поля в JobStatus

```typescript
interface JobStatus {
  job_id: string
  status: 'uploading' | 'pending' | 'running' | 'done' | 'error'
  progress: number
  message: string
  serial_numbers: string[]
  grafana_url?: string
  error?: string
  upload_size?: number      // NEW: Размер загруженных данных
  upload_speed?: number     // NEW: Скорость загрузки (MB/s)
  upload_progress?: number  // NEW: Прогресс загрузки
}
```

### Загрузка с chunking

```python
# Backend загружает файл чанками по 1MB
chunk_size = 1024 * 1024  # 1MB chunks

# С обновлением прогресса после каждого чанка
jobs[job_id]["upload_size"] = total_size
jobs[job_id]["upload_speed"] = speed_mbps
```

---

## 🚀 Как использовать

### 1. Запустите обновлённую систему

```bash
cd /data/projects/monitoring_VM_Grafana
docker compose up -d
```

### 2. Откройте Web UI

```
http://localhost:3001
```

или (если настроили реальный IP):

```
http://192.168.1.100:3001
```

### 3. Загрузите файл

1. Перетащите .zip файл в область загрузки
2. **Наблюдайте прогресс загрузки:**
   - Скорость: XX.XX MB/s
   - Загружено: XXX MB
3. После загрузки начнётся обработка
4. По завершении откройте Grafana

---

## 📋 Переменные окружения

### Обновлённый `.env` файл

```bash
# VictoriaMetrics
VM_PORT=8428
VM_RETENTION=6

# Grafana
GRAFANA_PORT=3000
GRAFANA_ADMIN_PASS=changeme
# ВАЖНО: Замените localhost на реальный IP для внешнего доступа
GRAFANA_URL=http://localhost:3000

# API
API_PORT=8000
MAX_UPLOAD_SIZE=10737418240  # 10GB
JOB_TIMEOUT=3600  # 1 hour (NEW)

# Web UI
WEB_PORT=3001
# ВАЖНО: Замените localhost на реальный IP для внешнего доступа
VITE_API_URL=http://localhost:8000
VITE_GRAFANA_URL=http://localhost:3000
```

---

## 🔄 Миграция с предыдущей версии

```bash
# 1. Остановить старые контейнеры
docker compose down

# 2. (Опционально) Создать .env с вашими настройками
nano .env

# 3. Пересобрать контейнеры
docker compose build --no-cache

# 4. Запустить
docker compose up -d

# 5. Проверить статус
docker compose ps
./TEST_API.sh
```

---

## 🧪 Тестирование

### Тест загрузки с прогрессом

```bash
# Загрузить тестовый файл
curl -X POST "http://localhost:8000/api/upload" \
  -F "file=@test.zip" \
  -v

# Получить job_id из ответа
JOB_ID="xxx-xxx-xxx"

# Мониторить статус (включая upload_speed)
watch -n 1 "curl -s http://localhost:8000/api/status/$JOB_ID | jq"
```

### Тест таймаута

```bash
# Установить короткий таймаут для теста
echo "JOB_TIMEOUT=60" >> .env  # 1 минута

# Перезапустить API
docker compose restart api

# Загрузить большой файл (> 1 минуты обработки)
# Задача должна прерваться с ошибкой таймаута
```

---

## 📈 Производительность

### Скорость загрузки

**Зависит от:**
- Скорости сети
- Размера файла
- Загрузки сервера

**Типичные значения:**
- Локальная сеть (1 Gbps): ~100-120 MB/s
- Wi-Fi (100 Mbps): ~10-12 MB/s
- Интернет (100 Mbps): ~8-10 MB/s

### Обработка файлов

**С флагом `--all-metrics`:**
- Парсинг: +20-30% времени (больше метрик)
- Импорт: +30-40% времени (больше данных)
- Результат: более полная картина производительности

**Пример:**
- Файл 500MB
- Раньше: ~3-4 минуты (DEFAULT метрики)
- Теперь: ~5-6 минут (ALL метрики)

---

## 🐛 Известные ограничения

1. **Прогресс загрузки:**
   - Обновляется каждый 1MB chunk
   - Для маленьких файлов (<10MB) может показаться мгновенным

2. **Таймаут:**
   - Применяется только к процессу обработки
   - Не применяется к самой загрузке файла

3. **Память:**
   - Большие файлы (>5GB) требуют больше времени
   - Рекомендуется увеличить `JOB_TIMEOUT` для файлов >2GB

---

## 📝 Changelog

### v1.1.0 (6 октября 2025)

**Added:**
- ✅ Прогресс-бар загрузки с скоростью
- ✅ Таймаут для задач обработки
- ✅ Поддержка реального IP адреса
- ✅ Статус "uploading"
- ✅ Отображение скорости загрузки

**Changed:**
- 🔧 Правильный URL дашборда Grafana
- 🔧 Парсинг --all-metrics по умолчанию
- 🔧 Улучшенная обработка ошибок

**Fixed:**
- 🐛 Зависшие задачи при ошибках
- 🐛 Неправильная ссылка на Grafana
- 🐛 Отсутствие feedback при загрузке

---

## 🎯 Следующие шаги

Рекомендации для дальнейших улучшений:

1. **Аутентификация**
   - JWT токены
   - Роли пользователей

2. **Персистентность**
   - Redis для job storage
   - PostgreSQL для истории

3. **Мониторинг**
   - Prometheus метрики
   - Grafana dashboard для API

4. **Масштабирование**
   - Celery workers
   - Кластер VictoriaMetrics

---

**Готово! Наслаждайтесь обновлениями! 🚀**

