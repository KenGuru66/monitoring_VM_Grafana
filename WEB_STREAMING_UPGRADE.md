# 🚀 Обновление Web Interface: Streaming Pipeline

**Дата:** 2025-10-07  
**Версия:** 2.0.0 → 2.1.0

---

## 📋 Что изменилось

### ✅ Веб-интерфейс теперь использует Streaming Pipeline

**Старый pipeline (`huawei_to_vm_pipeline.py`):**
- ❌ Создает промежуточные CSV файлы на диске
- ❌ Занимает много места (для 500GB лога → ~500GB CSV)
- ❌ Медленнее (два этапа: parse → import)

**Новый pipeline (`huawei_streaming_pipeline.py`):**
- ✅ Обрабатывает данные в памяти (streaming)
- ✅ НЕ создает CSV файлы (экономия места)
- ✅ Отправка напрямую в VictoriaMetrics
- ✅ Быстрее (один этап: parse+import)
- ✅ Поддержка очень больших файлов (500GB+)

---

## 🔧 Технические изменения

### 1. API Backend (`api/main.py`)

**Изменено:**
```python
# Было:
cmd = [
    "python3",
    "/app/huawei_to_vm_pipeline.py",
    "-i", str(zip_path),
    "-o", str(OUTPUT_DIR),  # ← CSV output директория
    "--vm-url", VM_IMPORT_URL,
    "--batch-size", "50000",
    "--all-metrics"
]

# Стало:
cmd = [
    "python3",
    "/app/huawei_streaming_pipeline.py",  # ← новый скрипт
    "-i", str(zip_path),
    # NO OUTPUT DIR - пишет сразу в VM!
    "--vm-url", VM_IMPORT_URL,
    "--batch-size", "100000",  # ← увеличен batch
    "--all-metrics"
]
```

**Progress Markers обновлены:**
```python
# Было:
progress_markers = {
    "Парсинг": 20,
    "CSV файлов:": 50,
    "Импорт": 60,
    "PIPELINE ЗАВЕРШЁН": 95
}

# Стало:
progress_markers = {
    "Processing TGZ files": 20,
    "Worker": 50,
    "metrics in": 70,
    "STREAMING PIPELINE COMPLETED": 95
}
```

### 2. Docker (`api/Dockerfile`)

**Добавлен:**
```dockerfile
COPY huawei_streaming_pipeline.py /app/huawei_streaming_pipeline.py
```

---

## 📊 Производительность

### Преимущества нового pipeline:

| Метрика | Старый | Новый | Улучшение |
|---------|--------|-------|-----------|
| **Disk Space** | 500GB CSV | 0 GB | **-100%** ✅ |
| **Memory Usage** | Low | Medium | Контролируемо |
| **Speed** | Baseline | Same/Faster | ✅ |
| **Large Files** | Limited | **500GB+** | ✅ |

### Тесты показали:

- **16 vCPU:** 1.33M metrics/sec
- **32 vCPU:** 1.80M metrics/sec
- **Batch 100k:** Оптимальный размер

**Для 500GB файла (~40 млрд строк):**
- **16 vCPU:** ~8.5 часов
- **32 vCPU:** ~6.3 часов

---

## 🎯 Как это работает

### Архитектура Streaming Pipeline

```
┌─────────────────────┐
│  Web Upload (ZIP)   │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│  Extract TGZ files  │  ← в памяти
│  (in-memory)        │
└──────────┬──────────┘
           ↓
┌─────────────────────────────────────┐
│  Multiprocessing Workers (30)       │
│  ┌───────┐ ┌───────┐ ┌───────┐     │
│  │Worker1│ │Worker2│ │Worker3│ ... │
│  │Parse  │ │Parse  │ │Parse  │     │
│  │TGZ    │ │TGZ    │ │TGZ    │     │
│  └───┬───┘ └───┬───┘ └───┬───┘     │
└──────┼─────────┼─────────┼─────────┘
       ↓         ↓         ↓
┌──────────────────────────────────────┐
│  Batch (100k metrics) → HTTP POST    │
└──────────────────┬───────────────────┘
                   ↓
        ┌──────────────────────┐
        │  VictoriaMetrics     │
        │  Direct Write        │
        │  (No intermediate    │
        │   CSV files!)        │
        └──────────────────────┘
```

### Преимущества архитектуры:

1. **NO Disk I/O для CSV** - все в памяти
2. **Parallel Processing** - 30 workers одновременно
3. **Streaming** - обрабатывает и отправляет по мере парсинга
4. **Efficient Batching** - 100k metrics per HTTP request

---

## 🚀 Применение изменений

### Обновление на сервере:

```bash
# 1. Pull последние изменения (если из Git)
git pull origin in-memory-processing

# 2. Пересборка API контейнера
docker compose build api

# 3. Перезапуск API
docker compose restart api

# 4. Проверка статуса
docker compose ps api
docker compose logs api --tail 20
```

### Проверка обновления:

```bash
# Проверить что API работает
curl http://localhost:8000/health

# Проверить версию
curl http://localhost:8000/ | jq .version
# Должно показать: "2.0.0" или выше
```

---

## 📝 Обратная совместимость

### Старый pipeline ВСЕ ЕЩЕ доступен!

Если нужно использовать старый pipeline (с CSV файлами):

```bash
# Запуск вручную
python3 huawei_to_vm_pipeline.py \
  -i "archive.zip" \
  -o "Data2csv/output" \
  --vm-url "http://localhost:8428/api/v1/import/prometheus" \
  --all-metrics
```

**Когда использовать старый pipeline:**
- Нужны CSV файлы для анализа
- Отладка данных
- Импорт в другие системы (не VM)

**Когда использовать новый pipeline:**
- ✅ Production загрузка в VictoriaMetrics
- ✅ Большие файлы (100GB+)
- ✅ Экономия дискового пространства
- ✅ Максимальная скорость

---

## 🎓 Рекомендации

### Для оптимальной производительности:

1. **vCPU:**
   - Production: 16 vCPU (cost-effective)
   - Срочная обработка: 32 vCPU (быстрее на 35%)

2. **Memory:**
   - Минимум: 16 GB
   - Рекомендуется: 32 GB
   - Для больших файлов: 64 GB

3. **Disk (для VictoriaMetrics):**
   - SSD: baseline
   - **NVMe: рекомендуется** (+30-50% speed)

4. **Настройки:**
   - `WORKER_CONCURRENCY=4` (API FastAPI workers)
   - `--batch-size 100000` (VictoriaMetrics batch)
   - `--all-metrics` (по умолчанию)

---

## 🐛 Troubleshooting

### Проблема: "huawei_streaming_pipeline.py not found"

**Решение:**
```bash
# Пересобрать API контейнер
docker compose build api --no-cache
docker compose restart api
```

### Проблема: Медленная обработка

**Проверить:**
```bash
# CPU утилизация
htop

# Memory
free -h

# Disk I/O (VictoriaMetrics)
iostat -x 1

# VictoriaMetrics логи
docker compose logs victoriametrics --tail 50
```

### Проблема: Out of Memory

**Решение:**
```bash
# Уменьшить количество workers в streaming pipeline
# В api/main.py добавить:
cmd = [
    "python3",
    "/app/huawei_streaming_pipeline.py",
    "-i", str(zip_path),
    "--vm-url", VM_IMPORT_URL,
    "--batch-size", "50000",  # ← уменьшить batch
    "-w", "15",  # ← уменьшить workers
    "--all-metrics"
]
```

---

## ✅ Чеклист обновления

- [x] Обновлен `api/main.py` - использует streaming pipeline
- [x] Обновлен `api/Dockerfile` - копирует `huawei_streaming_pipeline.py`
- [x] Пересобран Docker образ `api`
- [x] Перезапущен контейнер `api`
- [x] Проверен статус API (health check)
- [x] Обновлены progress markers
- [x] Batch size увеличен до 100k
- [x] Документация создана

---

## 📚 Дополнительные ресурсы

- `FINAL_COMPARISON.md` - Сравнение производительности 16 vs 32 vCPU
- `README.md` - Основная документация проекта
- `huawei_streaming_pipeline.py` - Исходный код streaming pipeline

---

**Status:** ✅ Completed  
**Next Steps:** Тестирование через веб-интерфейс с реальными данными
