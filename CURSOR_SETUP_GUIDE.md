# 🚀 Cursor AI Setup Guide для Huawei Storage Monitoring

## 📋 Что было сделано

### 1. ✅ `.cursorrules` файл создан

Файл содержит:
- 🎯 Обзор проекта и структуры
- 🐍 Python code standards (type hints, logging, error handling)
- ⚛️ React/TypeScript standards
- 📊 Data processing rules (метрики, ресурсы, unknown IDs)
- 🚀 Performance guidelines (streaming, batching, multiprocessing)
- 🐳 Docker & Infrastructure правила
- 📝 Documentation standards
- ✅ Pre-commit checklist
- 🚨 Common pitfalls и best practices

**Расположение:** `/data/projects/monitoring_VM_Grafana/.cursorrules`

### 2. ✅ Custom MCP Server создан

**Файл:** `/data/projects/monitoring_VM_Grafana/mcp_huawei_server.py`

**Возможности:**
- Просмотр всех массивов в VictoriaMetrics
- Проверка unknown метрик
- Статистика по метрикам для каждого массива
- Список processing jobs
- Health checks для Grafana и VictoriaMetrics
- Поиск недавно добавленных unknown метрик

### 3. ✅ Документация по MCP серверам

**Файл:** `/data/projects/monitoring_VM_Grafana/MCP_SERVERS_RECOMMENDATIONS.md`

Содержит рекомендации по:
- Docker MCP Server (обязательно!)
- Prometheus/VictoriaMetrics MCP
- Filesystem, SQLite, Git MCP серверы
- Примеры конфигураций
- Реальные сценарии использования

---

## 🔧 Быстрая настройка

### Шаг 1: Настройка MCP серверов в Cursor

#### Минимальная конфигурация (Docker MCP):

1. Откройте Cursor Settings (Ctrl+,)
2. Найдите "Model Context Protocol" или "MCP Servers"
3. Добавьте конфигурацию:

```json
{
  "mcpServers": {
    "docker": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-docker"]
    }
  }
}
```

#### Рекомендуемая конфигурация (Docker + Custom Huawei MCP):

```json
{
  "mcpServers": {
    "docker": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-docker"]
    },
    "huawei": {
      "command": "python3",
      "args": ["/data/projects/monitoring_VM_Grafana/mcp_huawei_server.py"]
    }
  }
}
```

### Шаг 2: Проверка установки

Перезапустите Cursor и проверьте:

```bash
# Тест Docker MCP (в терминале)
docker ps

# Тест Custom Huawei MCP
python3 /data/projects/monitoring_VM_Grafana/mcp_huawei_server.py list_arrays
```

### Шаг 3: Тестирование в Cursor

Попробуйте команды AI:
- "Покажи все запущенные Docker контейнеры"
- "Какие массивы сейчас в VictoriaMetrics?"
- "Есть ли unknown метрики?"
- "Покажи логи API контейнера"

---

## 📚 Примеры использования

### Сценарий 1: Отладка после загрузки данных

**Проблема:** Загрузил архив, но не вижу данных в Grafana

**Команды для AI:**
```
1. "Покажи логи API контейнера за последние 100 строк"
2. "Проверь, работает ли VictoriaMetrics"
3. "Список всех массивов в VM"
4. "Есть ли unknown метрики для массива 2102355THQFSQ?"
```

**Что AI сделает с MCP:**
- Прочитает логи через Docker MCP
- Проверит health через Custom MCP
- Покажет список массивов
- Найдет unknown метрики

### Сценарий 2: Performance debugging

**Проблема:** Парсинг работает медленно

**Команды для AI:**
```
1. "Покажи использование CPU и памяти контейнерами"
2. "Найди большие файлы в директории /data/projects/monitoring_VM_Grafana/temp"
3. "Прочитай последние 50 строк streaming_pipeline.log"
4. "Есть ли ошибки в логах API?"
```

### Сценарий 3: Работа с метриками

**Задача:** Понять, какие метрики используются

**Команды для AI:**
```
1. "Статистика по метрикам для массива 2102355THQFSQ"
2. "Покажи все unknown метрики"
3. "Какие ресурсы есть у массива?"
4. "Найди метрики, добавленные за последние 24 часа"
```

### Сценарий 4: Разработка нового функционала

**Задача:** Добавить новый endpoint в API

**Команды для AI:**
```
1. "Открой api/main.py и покажи структуру endpoints"
2. "Создай новый endpoint для получения статистики по job"
3. "Добавь обработку ошибок согласно .cursorrules"
4. "Обнови документацию в README.md"
```

**AI будет следовать правилам:**
- Type hints обязательны
- Structured logging
- Proper error handling
- Docstrings в Google style

---

## 🎯 Полезные команды для Custom MCP

### Проверка здоровья системы

```bash
# Через MCP в Cursor
"Проверь все сервисы: VictoriaMetrics, Grafana, API"

# Напрямую в терминале
python3 mcp_huawei_server.py check_vm
python3 mcp_huawei_server.py check_grafana
```

### Работа с массивами

```bash
# Список всех массивов
python3 mcp_huawei_server.py list_arrays

# Статистика по массиву
python3 mcp_huawei_server.py metric_stats 2102355THQFSQ

# Все метрики массива (первые 50)
python3 mcp_huawei_server.py array_metrics 2102355THQFSQ 50
```

### Поиск unknown метрик

```bash
# Все unknown во всей системе
python3 mcp_huawei_server.py check_unknown

# Unknown для конкретного массива
python3 mcp_huawei_server.py check_unknown 2102355THQFSQ

# Недавно добавленные unknown (за 48 часов)
python3 mcp_huawei_server.py recent_unknown 48
```

### Управление jobs

```bash
# Список всех jobs
python3 mcp_huawei_server.py list_jobs
```

---

## 💡 Tips & Tricks

### 1. Используйте контекст проекта

**❌ Плохо:**
```
"Как отправить метрики в базу данных?"
```

**✅ Хорошо:**
```
"Как отправить метрики в VictoriaMetrics используя streaming pipeline из huawei_streaming_pipeline.py?"
```

### 2. Ссылайтесь на .cursorrules

**✅ Хорошо:**
```
"Создай новую функцию для парсинга метрик, следуя правилам из .cursorrules"
"Добавь логирование согласно стандартам проекта"
```

### 3. Используйте MCP для быстрых проверок

**✅ Хорошо:**
```
"Перед началом разработки, проверь логи и убедись что все сервисы работают"
"После изменений в парсере, проверь есть ли новые unknown метрики"
```

### 4. Batch команды

**✅ Хорошо:**
```
"Выполни последовательно:
1. Проверь здоровье всех сервисов
2. Покажи список массивов
3. Найди unknown метрики
4. Покажи последние jobs"
```

---

## 🐛 Troubleshooting

### MCP сервер не работает

**Симптомы:** AI не может выполнить команды MCP

**Решение:**
```bash
# 1. Проверьте, что скрипт исполняемый
chmod +x /data/projects/monitoring_VM_Grafana/mcp_huawei_server.py

# 2. Проверьте, что requests установлен
pip3 install requests

# 3. Проверьте URL сервисов
python3 mcp_huawei_server.py check_vm
python3 mcp_huawei_server.py check_grafana

# 4. Проверьте логи Cursor
# Settings → Output → Model Context Protocol
```

### Docker MCP не работает

**Решение:**
```bash
# Установите Docker MCP сервер
npm install -g @modelcontextprotocol/server-docker

# Проверьте доступ к Docker
docker ps
```

### Custom MCP возвращает ошибки подключения

**Решение:**
```bash
# Проверьте, что сервисы запущены
docker compose ps

# Проверьте доступность
curl http://localhost:8428/health
curl http://localhost:3000/api/health
curl http://localhost:8000/health

# Если сервисы в Docker, используйте правильные URL
export VM_URL=http://victoriametrics:8428
export GRAFANA_URL=http://grafana:3000
export API_URL=http://api:8000
```

---

## 📖 Дополнительные ресурсы

### Документация проекта
- `README.md` - Общая документация
- `STREAMING_PIPELINE_README.md` - Streaming pipeline guide
- `MCP_SERVERS_RECOMMENDATIONS.md` - Детальная информация о MCP серверах
- `.cursorrules` - Правила кодирования

### Полезные файлы для изучения
- `huawei_streaming_pipeline.py` - Streaming processing
- `Data2csv/Huawei_perf_parser_v0.2_parallel.py` - CSV parser
- `api/main.py` - FastAPI backend
- `Data2csv/METRIC_DICT.py` - Словарь метрик (743 метрики)
- `Data2csv/RESOURCE_DICT.py` - Словарь ресурсов (51 ресурс)

### External Links
- [Cursor Docs](https://docs.cursor.com/)
- [MCP Protocol](https://modelcontextprotocol.io/)
- [VictoriaMetrics Docs](https://docs.victoriametrics.com/)
- [FastAPI Docs](https://fastapi.tiangolo.com/)

---

## ✅ Quick Start Checklist

После настройки, проверьте:

- [ ] `.cursorrules` файл существует и читается Cursor
- [ ] Docker MCP настроен и работает
- [ ] Custom Huawei MCP работает (test: `python3 mcp_huawei_server.py help`)
- [ ] AI может выполнять Docker команды
- [ ] AI может получать информацию от Custom MCP
- [ ] Все сервисы запущены (docker compose ps)
- [ ] VictoriaMetrics доступен (curl http://localhost:8428/health)
- [ ] Grafana доступен (curl http://localhost:3000/api/health)

---

## 🎉 Готово!

Теперь вы можете:
- ✅ Использовать AI assistant с контекстом проекта
- ✅ Быстро отлаживать через MCP серверы
- ✅ Следовать единым стандартам кодирования
- ✅ Автоматизировать рутинные проверки
- ✅ Эффективнее разрабатывать новый функционал

**Попробуйте прямо сейчас:**
```
"Покажи мне все массивы в системе и проверь есть ли у них unknown метрики"
```

---

**Создано:** October 2025
**Версия проекта:** 2.1.0
**Автор:** AI Assistant + Cursor

