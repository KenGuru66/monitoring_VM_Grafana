# TODO - Huawei Storage Monitoring Project

## 🟡 Средний приоритет

### 2. Добавить кэширование metadata для /api/arrays

**Проблема:**
При каждом запросе `/api/arrays` делается batch запрос к VictoriaMetrics для получения scrape_interval. При большом количестве массивов это может быть медленно.

**Что нужно сделать:**
- Добавить in-memory кэш для metadata (TTL ~5 минут)
- Или использовать Redis для кэширования

---

### 3. Добавить индикатор загрузки при клике на Grafana (Home page)

**Проблема:**
При клике на кнопку "Grafana" на главной странице происходит запрос `/api/array/{sn}/timerange` который занимает 1-3 секунды. Пользователь не видит что что-то происходит.

**Что нужно сделать:**
- Добавить loading spinner на кнопку во время запроса timerange
- Или показать toast notification "Загрузка..."

---

## 🟢 Низкий приоритет

### 4. Кэширование timerange для мгновенного открытия Grafana с правильным периодом

**Текущее поведение:**
Кнопка "Открыть" сразу открывает Grafana с дефолтным периодом (6 часов). Это работает, но не показывает полный диапазон данных массива.

**Желаемое поведение:**
Открывать Grafana с автоматическим from/to по реальным данным в VictoriaMetrics.

**Варианты реализации:**

| Подход | Плюсы | Минусы |
|--------|-------|--------|
| **1. Сохранять при импорте** | Самый эффективный — данные уже парсятся | Нужно модифицировать streaming_pipeline |
| **2. Redis** | Быстрый, масштабируемый | Дополнительный сервис в docker-compose |
| **3. Фоновый worker** | Независимый процесс | Дублирование запросов, overhead |

**Рекомендуемый подход — Сохранять при импорте:**
1. В `streaming_pipeline.py` отслеживать min/max timestamp при парсинге
2. После успешного импорта — сохранять в JSON файл или отправлять в API endpoint
3. При открытии Grafana — читать из кэша (мгновенно)

```
СХД → .tgz → perf_watcher → streaming_pipeline → VictoriaMetrics
                                    ↓
                             сохранить timerange
                                    ↓
                             timerange_cache.json (или Redis)
```

---

### 5. Оптимизировать получение time_from/time_to через VictoriaMetrics API

**Текущее решение:**
Используется export API который медленный для массивов с большим количеством данных (30+ сек timeout).

**Возможные улучшения:**
- Использовать `min_over_time` / `max_over_time` функции в PromQL
- Использовать `/api/v1/series` с `start`/`end` параметрами

---

---

## ✅ Выполнено (2025-12-03)

- [x] **Batch Upload в Web UI** — параллельная загрузка + последовательная обработка архивов
  - Поддержка множества .zip и .7z файлов
  - Pipeline: Uploading → Queued → Processing → Done
  - Real-time статистика и логи для каждого файла

## ✅ Выполнено (2025-12-01)

- [x] Merge feature/local-dev-support в main
- [x] Сохранена поддержка .7z архивов  
- [x] Реструктуризация проекта (parsers/, tools/, parsers/dictionaries/)
- [x] Docker настроен для production (docker-compose.yml + docker-compose.prod.yml)
- [x] Ссылка на Grafana после парсинга содержит правильный временной диапазон (from/to)
- [x] Оптимизация /api/arrays - загрузка за <1 сек вместо 26+ сек
- [x] Добавлен endpoint /api/array/{sn}/timerange для on-demand получения временного диапазона
- [x] Fix race condition: grafana_url устанавливается ДО status="done"
- [x] **Grafana: фильтрация Element по Resource** — изменён `refresh` для переменных Resource и Element с `2` (On time range change) на `1` (On dashboard load). Теперь при выборе конкретного Resource список Element автоматически обновляется и показывает только релевантные элементы.

