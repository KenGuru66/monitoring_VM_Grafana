# 📖 Обработка неизвестных ID метрик и ресурсов

## 🎯 Описание

Улучшенный парсер `Huawei_perf_parser_v0.2_parallel.py` автоматически обрабатывает **неизвестные ID** метрик и ресурсов, которых нет в словарях `METRIC_DICT.py` и `RESOURCE_DICT.py`.

### Как это работает:

1. ✅ **Парсер обрабатывает ВСЕ данные** (не пропускает неизвестные ID)
2. ✅ **Записывает их как UNKNOWN** в CSV файл
3. ✅ **Логирует предупреждения** в лог-файл
4. ✅ **Скрипт обновления** автоматически добавляет их в словари

---

## 🚀 БЫСТРЫЙ СТАРТ

### 1. Парсинг данных с автоматической обработкой UNKNOWN

```bash
cd /data/projects/monitoring_VM_Grafana/Data2csv

# Запуск парсера со всеми метриками и ресурсами
python3 Huawei_perf_parser_v0.2_parallel.py \
    -i logs/archive.zip \
    -o output \
    --all-metrics \
    -l log
```

### 2. Проверка на наличие UNKNOWN ID

```bash
# Проверка в CSV файлах
grep -i "UNKNOWN" output/*.csv

# Проверка в логах
grep -i "unknown" log/process_perf_files.log
```

### 3. Автоматическое обновление словарей

```bash
# С подтверждением (интерактивный режим)
python3 update_dictionaries.py output

# Автоматически без подтверждения
python3 update_dictionaries.py output --auto
```

---

## 📋 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### Пример 1: Обработка новых данных

```bash
# Шаг 1: Парсинг
python3 Huawei_perf_parser_v0.2_parallel.py \
    -i /path/to/new_data.zip \
    -o output/new_data \
    --all-metrics

# Шаг 2: Проверка логов
grep "unknown.*IDs" output/new_data/*.log

# Шаг 3: Обновление словарей (если найдены UNKNOWN)
python3 update_dictionaries.py output/new_data --auto

# Шаг 4: Повторный парсинг (опционально, для проверки)
python3 Huawei_perf_parser_v0.2_parallel.py \
    -i /path/to/new_data.zip \
    -o output/new_data_v2 \
    --all-metrics
```

### Пример 2: Только анализ без обновления

```bash
# Запустить скрипт без подтверждения (только анализ)
python3 update_dictionaries.py output

# Нажать 'n' при запросе подтверждения
```

---

## 🔍 ДЕТАЛЬНОЕ ОПИСАНИЕ

### Формат UNKNOWN записей

**В CSV файле:**
```
UNKNOWN_RESOURCE_12345;Read bandwidth (MB/s);Instance1;100;2025-10-13T00:00:00Z;1760302800.0
Controller;UNKNOWN_METRIC_98765;CTE0.A;150;2025-10-13T00:05:00Z;1760303100.0
UNKNOWN_RESOURCE_55555;UNKNOWN_METRIC_44444;TestPort;75;2025-10-13T00:10:00Z;1760303400.0
```

**В логах парсера:**
```
[WARNING] Found 2 unknown resource IDs in file.dat: ['12345', '55555']
[WARNING] Found 2 unknown metric IDs in file.dat: ['44444', '98765']
```

**В словарях после обновления:**
```python
# RESOURCE_DICT.py
RESOURCE_NAME_DICT = {
    ...
    "12345": "UNKNOWN_RESOURCE_12345",  # AUTO-ADDED
    "55555": "UNKNOWN_RESOURCE_55555",  # AUTO-ADDED
}

# METRIC_DICT.py
METRIC_NAME_DICT = {
    ...
    "44444": "UNKNOWN_METRIC_44444",  # AUTO-ADDED
    "98765": "UNKNOWN_METRIC_98765",  # AUTO-ADDED
}
```

---

## 🛠️ ПАРАМЕТРЫ СКРИПТА update_dictionaries.py

```bash
python3 update_dictionaries.py [OPTIONS] [CSV_DIRECTORY]
```

### Аргументы:

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `CSV_DIRECTORY` | Путь к директории с CSV файлами | Авто-определение |
| `--auto`, `-y` | Автоматически применить без подтверждения | False |
| `--help`, `-h` | Показать справку | - |

### Примеры:

```bash
# Авто-определение директории
python3 update_dictionaries.py

# Указать конкретную директорию
python3 update_dictionaries.py /path/to/csv/files

# Автоматическое применение
python3 update_dictionaries.py output --auto
python3 update_dictionaries.py -y  # краткая форма
```

---

## 📊 СТАТИСТИКА И ОТЧЕТЫ

### Вывод скрипта update_dictionaries.py:

```
================================================================================
АВТОМАТИЧЕСКОЕ ОБНОВЛЕНИЕ СЛОВАРЕЙ
================================================================================

🔍 Найдено 1 CSV файлов
================================================================================
📖 Анализирую файл: output/data.csv
  Обработано 100,000 строк...
  Обработано 200,000 строк...

================================================================================
РЕЗУЛЬТАТЫ АНАЛИЗА
================================================================================

📋 Найдено неизвестных ресурсов: 3
  - ID 12345
  - ID 55555
  - ID 99999

📋 Найдено неизвестных метрик: 2
  - ID 44444
  - ID 98765

📖 Читаю текущие словари...
  Текущих ресурсов: 51
  Текущих метрик: 743

================================================================================
ПЛАНИРУЕМЫЕ ИЗМЕНЕНИЯ
================================================================================

📝 Будет добавлено в RESOURCE_DICT.py: 3 записей
    "12345": "UNKNOWN_RESOURCE_12345"
    "55555": "UNKNOWN_RESOURCE_55555"
    "99999": "UNKNOWN_RESOURCE_99999"

📝 Будет добавлено в METRIC_DICT.py: 2 записей
    "44444": "UNKNOWN_METRIC_44444"
    "98765": "UNKNOWN_METRIC_98765"

Применить изменения? [y/N]: y

================================================================================
ОБНОВЛЕНИЕ СЛОВАРЕЙ
================================================================================

✅ Добавлено 3 записей в RESOURCE_DICT.py
✅ Добавлено 2 записей в METRIC_DICT.py

================================================================================
✅ ОБНОВЛЕНИЕ ЗАВЕРШЕНО!
================================================================================

📊 Итого:
  Ресурсов было: 51
  Ресурсов стало: 54
  Добавлено: 3

  Метрик было: 743
  Метрик стало: 745
  Добавлено: 2

💡 Рекомендация:
   1. Проверьте обновленные словари
   2. Перезапустите парсер на тех же данных
   3. Убедитесь, что UNKNOWN ID больше нет
```

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

### 1. Именование UNKNOWN ID

Неизвестные ID автоматически получают имена вида:
- `UNKNOWN_RESOURCE_<ID>` для ресурсов
- `UNKNOWN_METRIC_<ID>` для метрик

Где `<ID>` - это числовой идентификатор из бинарных логов.

### 2. Ручное редактирование после добавления

После автоматического добавления **рекомендуется**:

1. Найти описание в документации Huawei
2. Заменить `UNKNOWN_*` на правильное имя
3. Добавить комментарий с описанием

**Пример:**

```python
# Было (автоматически):
"12345": "UNKNOWN_RESOURCE_12345",  # AUTO-ADDED

# Стало (после ручного редактирования):
"12345": "NVMe Port",  # NVMe front-end port
```

### 3. Резервное копирование

Скрипт **НЕ создает** автоматические резервные копии словарей.

**Рекомендуется** перед обновлением:

```bash
# Создать резервные копии
cp METRIC_DICT.py METRIC_DICT.py.backup
cp RESOURCE_DICT.py RESOURCE_DICT.py.backup

# Запустить обновление
python3 update_dictionaries.py --auto

# Проверить результат
diff METRIC_DICT.py METRIC_DICT.py.backup
diff RESOURCE_DICT.py RESOURCE_DICT.py.backup

# Восстановить при необходимости
# mv METRIC_DICT.py.backup METRIC_DICT.py
# mv RESOURCE_DICT.py.backup RESOURCE_DICT.py
```

### 4. Git и контроль версий

**Рекомендуется** использовать Git для отслеживания изменений:

```bash
# Перед обновлением
git status
git diff METRIC_DICT.py RESOURCE_DICT.py

# После обновления
git add METRIC_DICT.py RESOURCE_DICT.py
git commit -m "Auto-added unknown IDs: RESOURCE_12345, METRIC_44444, etc."
```

---

## 🔧 TROUBLESHOOTING

### Проблема: Скрипт не находит CSV файлы

**Решение:**
```bash
# Явно указать путь к директории
python3 update_dictionaries.py /полный/путь/к/output
```

### Проблема: "Permission denied" при записи в словари

**Решение:**
```bash
# Проверить права доступа
ls -l METRIC_DICT.py RESOURCE_DICT.py

# Изменить права при необходимости
chmod u+w METRIC_DICT.py RESOURCE_DICT.py
```

### Проблема: Дублирующиеся записи в словарях

**Причина:** Скрипт был запущен несколько раз на одних и тех же данных.

**Решение:**
Скрипт **автоматически проверяет** наличие ID в словарях и не добавляет дубликаты.
Если всё же возникли проблемы:

```bash
# Восстановить из резервной копии
cp METRIC_DICT.py.backup METRIC_DICT.py

# Или очистить дубликаты вручную
```

---

## 📚 СВЯЗАННЫЕ ФАЙЛЫ

- `Huawei_perf_parser_v0.2_parallel.py` - Основной парсер (с поддержкой UNKNOWN)
- `update_dictionaries.py` - Скрипт автообновления словарей
- `test_unknown_ids.py` - Скрипт для анализа UNKNOWN в CSV
- `METRIC_DICT.py` - Словарь метрик (743 записи)
- `RESOURCE_DICT.py` - Словарь ресурсов (51 запись)
- `IMPROVEMENTS_SUMMARY.md` - Полный отчет об улучшениях

---

## ✅ BEST PRACTICES

1. **Перед парсингом новой модели:**
   - Запустите парсер с `--all-metrics`
   - Проверьте логи на UNKNOWN
   - Обновите словари
   - Перезапустите парсер для проверки

2. **При обнаружении UNKNOWN:**
   - Не удаляйте данные из CSV
   - Обновите словари автоматически
   - Найдите описание в документации
   - Замените UNKNOWN на правильные имена

3. **Регулярное обслуживание:**
   - Периодически проверяйте словари на UNKNOWN_*
   - Обновляйте описания из официальной документации
   - Ведите changelog изменений словарей

4. **Автоматизация:**
   ```bash
   #!/bin/bash
   # Скрипт полной обработки с автообновлением
   
   # 1. Парсинг
   python3 Huawei_perf_parser_v0.2_parallel.py -i $1 -o output --all-metrics
   
   # 2. Автообновление словарей
   python3 update_dictionaries.py output --auto
   
   # 3. Повторный парсинг (если были UNKNOWN)
   if grep -q "UNKNOWN" output/*.csv; then
       echo "Найдены UNKNOWN, перезапускаю парсер..."
       python3 Huawei_perf_parser_v0.2_parallel.py -i $1 -o output_v2 --all-metrics
   fi
   ```

---

## 📞 ПОДДЕРЖКА

Если вы столкнулись с проблемами:

1. Проверьте логи парсера: `log/process_perf_files.log`
2. Запустите `test_unknown_ids.py` для анализа
3. Проверьте формат CSV файлов
4. Убедитесь в правах доступа к словарям

---

**Дата создания:** 16 октября 2025  
**Версия:** 1.0  
**Статус:** ✅ Готово к использованию

