# 🔬 Руководство по валидации парсера Huawei Performance Data

## Цель
Сравнить результаты работы оригинального парсера (Python 2) и нового параллельного парсера (Python 3) со всеми метриками и ресурсами.

---

## 📊 Текущее состояние

### Словари метрик и ресурсов

**Оригинальный Parse_Perf_Files.py (встроенные словари):**
- ❌ 563 метрики (устаревший)
- ❌ 45 ресурсов (устаревший)

**Новые METRIC_DICT.py + RESOURCE_DICT.py:**
- ✅ **743 метрики** (+180 новых!)
- ✅ **51 ресурсов** (+6 новых!)

### Новые метрики включают:
- Метрики в микросекундах (369, 370, 371, 384, 385, 392-405 и др.)
- Расширенные метрики NFS/CIFS (703-766)
- VAAI метрики (1154-1191)
- ODX метрики (1162-1181)
- DataTurbo метрики (1263-1273)
- CPU партиционирование (1296-1299)
- Дедупликация (1332-1338)
- И многие другие

### Новые ресурсы:
- SmartCache Pool (218)
- Remote Device (224)
- SmartMigration (253)
- DataTurbo Controller (659)
- IP Replication Link (20486)
- ETH_EXP_Port (57867)

---

## 🚀 Пошаговая инструкция

### Шаг 1: Установка Python 2.7

```bash
# Вариант 1: Через deadsnakes PPA (рекомендуется для Ubuntu 24.04)
sudo add-apt-repository ppa:deadsnakes/ppa
sudo apt update
sudo apt install -y python2.7

# Вариант 2: Прямая установка (если доступен)
sudo apt update
sudo apt install -y python2 python2-dev

# Проверка установки
python2 --version
# или
python2.7 --version
```

**Ожидаемый вывод:**
```
Python 2.7.18
```

---

### Шаг 2: Запуск оригинального парсера

```bash
cd /data/projects/monitoring_VM_Grafana/Data2csv

# Запуск с ВСЕМИ метриками и ресурсами из новых словарей
bash run_original_parser.sh
```

**Что делает скрипт:**
1. Извлекает все ключи из RESOURCE_DICT.py и METRIC_DICT.py (743 метрики + 51 ресурс)
2. Распаковывает ZIP архив с тестовыми данными
3. Запускает Parse_Perf_Files.py с полным набором метрик и ресурсов
4. Сохраняет результат в `output_test/original_py2/`

**Примерное время выполнения:** 5-15 минут (однопоточный режим)

---

### Шаг 3: Ваш новый парсер уже запущен!

Вы уже запустили новый парсер:
```bash
cd /data/projects/monitoring_VM_Grafana/Data2csv
python3 Huawei_perf_parser_v0.2_parallel.py \
    -i logs/Storage_History_Performance_Files.zip \
    -o output_test/parallel_py3 \
    --all-metrics \
    -w 4
```

**Результат:**
- ✅ 63,270,303 строк
- ✅ Размер: 6.1 GB
- ✅ Время: ~3 минуты (4 потока)

---

### Шаг 4: Сравнение результатов

```bash
cd /data/projects/monitoring_VM_Grafana/Data2csv

# Запуск скрипта сравнения
python3 compare_results.py
```

**Что проверяет скрипт:**

1. ✅ **Общее количество строк**
   - Должно совпадать точно

2. ✅ **Уникальные комбинации Resource + Metric**
   - Выявляет метрики, которые есть только в одном из парсеров

3. ✅ **Количество строк для каждой комбинации**
   - Проверяет, что для каждого Resource+Metric одинаковое количество записей

4. ✅ **Выборочное сравнение значений**
   - Сравнивает фактические значения метрик и временные метки
   - Проверяет точность парсинга бинарных данных

**Ожидаемый результат:**
- ✅ **ПОЛНОЕ СОВПАДЕНИЕ** - если оба парсера работают идентично
- ⚠️  **МИНИМАЛЬНЫЕ РАЗЛИЧИЯ** (<1%) - допустимо из-за округления/форматирования
- ❌ **СУЩЕСТВЕННЫЕ РАЗЛИЧИЯ** (>1%) - требуется проверка логики

---

## 📋 Чеклист валидации

- [ ] Python 2.7 установлен и работает
- [ ] Оригинальный парсер запущен успешно
- [ ] Новый парсер запущен успешно
- [ ] Скрипт сравнения выполнен
- [ ] Результаты проанализированы

---

## 🔍 Интерпретация результатов

### Сценарий 1: Полное совпадение ✅
```
ИТОГОВЫЙ ВЫВОД:
✅ ПОЛНОЕ СОВПАДЕНИЕ!
   Ваш новый скрипт (Python3) полностью корректен и
   дает ИДЕНТИЧНЫЕ результаты с оригинальным (Python2).
```

**Действие:** Можно смело использовать новый парсер!

---

### Сценарий 2: Минимальные различия ⚠️
```
ИТОГОВЫЙ ВЫВОД:
⚠️  МИНИМАЛЬНЫЕ РАЗЛИЧИЯ
   Различия пренебрежимо малы (<1%).
   Вероятно, связаны с округлением или форматированием.
```

**Возможные причины:**
- Разница в формате временных меток (Python 2 vs Python 3)
- Округление чисел с плавающей точкой
- Порядок обработки файлов

**Действие:** Проверить конкретные различия в выводе скрипта, обычно не критично.

---

### Сценарий 3: Существенные различия ❌
```
ИТОГОВЫЙ ВЫВОД:
❌ СУЩЕСТВЕННЫЕ РАЗЛИЧИЯ
   Требуется дополнительная проверка логики парсинга.
```

**Возможные причины:**
- Ошибка в логике парсинга бинарных данных
- Неправильная обработка struct.unpack
- Проблемы с кодировкой строк (bytes vs str в Python 3)

**Действие:** Проверить детали в выводе скрипта и исправить код.

---

## 🛠️ Полезные команды

### Быстрая проверка результатов

```bash
cd /data/projects/monitoring_VM_Grafana/Data2csv

# Количество строк
wc -l output_test/original_py2/*.csv
wc -l output_test/parallel_py3/*.csv

# Размеры файлов
ls -lh output_test/original_py2/
ls -lh output_test/parallel_py3/

# Первые строки
head -20 output_test/original_py2/*.csv
head -20 output_test/parallel_py3/*.csv

# Уникальные метрики в оригинале
cut -d';' -f1,2 output_test/original_py2/*.csv | sort -u | wc -l

# Уникальные метрики в новом
cut -d';' -f1,2 output_test/parallel_py3/*.csv | sort -u | wc -l
```

---

## 📝 Примечания

1. **Формат вывода обоих парсеров одинаковый:**
   ```
   Resource;Metric;InstanceName;Value;Time;UnixTime
   ```

2. **Оба парсера обрабатывают одни и те же бинарные файлы:**
   - Читают заголовки
   - Парсят структуру Map
   - Извлекают метрики с помощью struct.unpack
   - Генерируют временные метки

3. **Новый парсер имеет преимущества:**
   - ✅ Параллельная обработка (в 4-30 раз быстрее)
   - ✅ Поддержка ZIP архивов напрямую
   - ✅ Более полный набор метрик и ресурсов
   - ✅ Python 3 (современный, поддерживается)
   - ✅ Лучшая обработка памяти

---

## 🎯 Итоговая цель

После выполнения всех шагов у вас будет:

1. ✅ **Подтверждение корректности** нового парсера
2. ✅ **Уверенность в данных** - значения метрик идентичны
3. ✅ **Готовность к продакшену** - можно использовать новый парсер

---

## 💡 Дополнительная информация

Новый парсер (**Huawei_perf_parser_v0.2_parallel.py**) является **УЛУЧШЕННОЙ И РАСШИРЕННОЙ** версией оригинального Parse_Perf_Files.py:

- Содержит все 563 метрики из оригинала
- ПЛЮС 180 новых метрик
- Содержит все 45 ресурсов из оригинала
- ПЛЮС 6 новых ресурсов
- Использует Python 3 (безопаснее и поддерживается)
- Работает в 4-30 раз быстрее благодаря параллелизму

**Рекомендация:** После валидации переходите на новый парсер полностью!


